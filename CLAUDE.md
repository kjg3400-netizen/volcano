# 볼케이노 — 공통 규약

이 파일은 **모든 채널에 공통인 것**만 담는다. 채널별 상세 규격은 각 `ref_*/CLAUDE.md` 에 있고,
아래 표대로 **일을 시작하기 전에 먼저 읽는다.** 여기에 채널별 규격을 다시 복사하지 마라 —
같은 규칙이 두 곳에 있으면 한쪽이 반드시 낡는다.

## ★상세는 채널별 파일에 있다 — 부르는 말에 맞는 것을 먼저 읽어라

| 사장님이 이렇게 부르시면 | 먼저 읽을 파일 |
|---|---|
| `경제 소재 뽑아 줘` · `오늘 뭐 만들까` · `<번호>번으로 가` | `ref_econ/CLAUDE.md` |
| `일본 경제 소재 뽑아 줘` · `쇼피드 뭐 만들까` | `ref_jpecon/CLAUDE.md` |
| `커뮤 소재 뽑아 줘` · `오늘 커뮤에 뭐 떴냐` | `ref_comm/CLAUDE.md` |
| `일본 커뮤 소재 뽑아 줘` · `일본 커뮤에 뭐 떴냐` | `ref_jpcomm/CLAUDE.md` |
| `<링크> 일본판 뇌전구로 줘` | `ref_jp/CLAUDE.md` |
| `<인물> 인물형으로 줘` · `<링크> 인물형` | `ref_person/CLAUDE.md` |
| `<클립> 축구로 줘` · `잭제이로 줘` | `ref_jjack/CLAUDE.md` |
| `<링크> 골프` · `짧뷰로 줘` | `ref_nono/CLAUDE.md` |
| `<클립> 칩칩으로 줘` · `칩칩`(소재 찾기) · `랑카 뭐 만들까` | `ref_chipchip/CLAUDE.md` |
| `<클립> 매일일보형으로 줘` | `ref_maeil/CLAUDE.md` |
| `<클립> 정치형으로 줘` · `<링크> 정치로` | `ref_poli/CLAUDE.md` |
| `축구 소재` · `짹짹 뭐 만들까` · `골프 소재` · `짧뷰 뭐 만들까` | `ref_sport/CLAUDE.md` |
| `영어학습형` · `배치 100편` | `ref_engl/CLAUDE.md` |
| 텔레그램 봇 · 폰 알림 · 주문 · 창고 | `ref_notify/CLAUDE.md` |

- **한국어 뇌전구 회차**(경제·커뮤 소재로 굽는 것)는 따로 파일이 없다 — 아래 「볼케이노 채널 작업 규약」이 전부다
- **표에 없는 새 형식**을 요청받으면 역설계하지 말고 `hitomi_downloaded_youtube\지침서모음\` 부터 뒤진다 (지침서 12개가 거기 있다)

---

# ★작업 방식 — 토큰과 시간을 아끼는 규칙 (사장님 지시 2026-08-25)

제작 규격과 별개로 **모든 작업에 항상 적용된다.**

## 확인은 한 번만 한다

- **이번 대화에서 이미 확인한 것을 다시 확인하지 마라.** 같은 파일 다시 읽기,
  같은 `grep`·`cat`·`git status` 다시 돌리기 — 앞의 결과를 그대로 쓴다
- **성공한 명령의 로그를 통째로 다시 읽지 마라.** `PASS` 한 줄이면 충분하다.
  오류가 났을 때만 그 주변을 더 본다
- 전체 저장소 스캔(`find` 로 전 파일 나열 · 모든 `.py`·`.md` 읽기)을 기본으로 하지 마라.
  아는 파일부터 보고 없을 때만 조금씩 넓힌다. Explore Agent 도 기본이 아니다

## 기다릴 때 두드리지 않는다

- `ps`·`tail`·`cat`·`grep` 을 몇 초마다 반복해 "아직 끝났나" 보지 마라.
  오래 걸리는 것은 백그라운드로 띄우면 끝날 때 알려 온다
- 렌더·빌드가 CPU 를 먹는 것은 정상이다. 그 이유만으로 죽이거나 다시 돌리지 마라

## 고칠 때는 그 부분만 고친다

- 검사 하나가 틀렸다고 처음부터 다시 만들지 마라 — 자막이면 자막만, 슬롯 하나면
  그 슬롯만 다시 굽는다. `cache/img`·`cache/tts` 가 그러라고 있는 것이다
- 렌더 전에 알 수 있는 것(길이·컷 수·자막 폭·파일 유무·밈 배정)은 **렌더 전에** 본다.
  값 하나 틀려서 ffmpeg 을 통째로 다시 돌리는 일이 없어야 한다

## 만들기 전에 있는지부터 본다

- 같은 목적의 helper·check·cache 를 새로 만들지 마라. 위 표의 채널 파일과 `ref_*` 를
  먼저 보고, 있으면 그 파일에 최소한으로 덧댄다. `foo_check2.py` 꼴로 늘리지 않는다

---

# ★사장님 채널 지도 (2026-08-21 확인)

| 갈래 | 한국 | 일본 | 소재 자동화 |
|---|---|---|---|
| 경제 | **숏단지** `UCBbTfTx1XafCl6JTJ2KgNcg` | **숏피드(ショーフィ)** `UClyn9gKhLeA4OOJclpjGm1Q` | ✅ `ref_econ` · `ref_jpecon` |
| 커뮤 | **방탐** (뇌전구커뮤) | **능구렁이** | ✅ `ref_comm` · `ref_jpcomm` |
| 축구 | **짹짹 (JJACK_J)** 구독 6,550 | **神ショーツ** | ❌ |
| 춤·스토리 | **칩칩** `UCMkYFJ7J5ELuhQ-lWKGMlXA` 구독 2,950 | **ランカー** | ❌ |
| 골프 | **짧뷰** `@짧뷰노노` 구독 1,840 | — | ❌ |

## ★두 부류는 소재 성격이 근본부터 다르다

- **글 채널** (경제·커뮤) — 네이버·야후·커뮤에 **공개된 반응 수치**가 있다.
  그래서 긁어서 재고 점수를 매길 수 있다. 자동화가 되는 이유가 이것이다.
- **클립 채널** (축구·골프·춤) — 소재가 **남의 영상**이다. 반응 수치를 긁을 데가 없고,
  `yt-dlp` 로는 쇼츠 발굴이 막혀 있다([[ytdlp-shorts-discovery-blocked]]) —
  검색·해시태그가 전부 롱폼만 준다. **되는 건 채널 `/shorts` 카탈로그뿐이다.**

  → 그래서 클립 채널의 발굴기는 **소재를 길어 오는 채널 목록**이 있어야 만들 수 있다.
    사장님이 평소 어디서 클립을 구해 오시는지가 입력값이다. 그 목록만 있으면
    매일 카탈로그를 받아 **조회 급등**을 잡아내는 방식이 된다.
  → 보조 수단으로 `ref_comm --only` 갈래 필터가 있다(커뮤에 뜬 화제 클립을 잡는다).

## 채널별 성과 (2026-08-21)

| 채널 | 구독 | 조회 중앙 | 최고 |
|---|--:|--:|--:|
| 짹짹 | 6,550 | 31,000 | 3,700,000 |
| 칩칩 | 2,950 | 22,000 | 2,100,000 |
| 짧뷰 | 1,840 | 35,000 | 6,600,000 |
| 숏단지 | 20 | 4,199 | 16,354 |
| 숏피드 | 12 | 5,643 | 7,948 |

---

# ★★작업 폴더는 채널별로 나뉘어 있다 (사장님 지시 2026-08-24)

회차 workdir 를 **볼케이노 루트에 파지 마라** — 반드시 채널 폴더 아래에 판다.
완성본은 지금처럼 `바탕화면\볼케이노 완성본\` 한곳에 모인다 (deliver 쪽은 그대로).

| 채널 폴더 | 들어가는 것 |
|---|---|
| `뇌전구_한국\` | 숏단지 경제 `work_econ_*` · 방탐 커뮤 `work_comm_*` · 그 밖의 한국어 뇌전구 회차 |
| `뇌전구_일본\` | 숏피드 경제 `work_jp_*` · 일본 커뮤 `work_jpcomm_*` · 일본판 뇌전구 회차 |
| `쇼핑\` `명화관\` `린박스\` `웃긴버거\` `어랍숏\` `인물형\` | 나머지 볼케이노 프리셋 회차 |
| `축구\` `골프\` `칩칩\` `정치\` `매일일보\` `리피\` `영어학습\` | 로컬 빌더 채널 회차 |

- 도구(`ref_*`·`runner`·`myp`)와 공용 자산(`cache`·`fonts`·`pepe`·`bgm`·`sfx_norm`·`footage`)은 루트 그대로다
- brief 3종(econ·jpecon·comm)은 채널 폴더 안에 workdir 를 알아서 판다 (2026-08-24 수정)
- `deliver_sweep.py`(Stop 훅)는 두 단계까지 내려가 `out/` 을 찾는다 — 채널 폴더 안 workdir 는
  자동으로 줍지만, **채널 폴더 안에 폴더를 한 겹 더 파면(세 단계) 훅이 못 본다.** 파지 마라
- ★루트에 `work_*` 가 새로 보이면 흘린 것이다 — **다른 세션이 굽고 있지 않은지 확인한 뒤**
  채널 폴더로 옮기고, `delivered.json` 안의 `src` 절대경로도 같이 고쳐라. 안 고치면
  Stop 훅이 '안 나간 것'으로 착각해 사장님이 지우신 완성본을 되살린다
- 옛 회차 폴더 안의 일회용 스크립트에 박힌 절대경로는 이동(2026-08-24) 전 것이라 낡았다 —
  재편집할 때 그 파일만 고친다. 활성 도구(정치 vsheet·vzoom·recheck, 매일일보 검증 3종·fontmatch,
  칩칩 probe_trend, 인물형 deliver_both)는 이미 고쳐 놨다
- `kenchiku_jp\`(建築 일본판)는 원래부터 채널 폴더 꼴이라 그대로다. `work_ch_map`·`work_ch_545429` 는
  채널 공통 조사라 루트에 남겼다

---

# 볼케이노 채널 작업 규약

## 영상 길이 상한 — 35초 (절대)

**35초를 넘기면 안 된다.** 30초 언저리가 이상적이다.

볼케이노 MCP 서버는 줄 단위 검증(한 줄 5어절·화면 폭)만 하고 **총 길이는 검증하지 않는다.**
길이는 전적으로 대본 작성자가 정하므로, 아래 게이트를 직접 통과시켜야 한다.

### 계산식

실측 7회차에서 **총 길이 ≈ 총 글자수(공백 제외, 오프닝 카드 포함) × 0.134초** 로 일정하다(편차 ±2%).

| 목표 | 총 글자수(카드 포함) |
|---|---|
| 30초 | 224자 |
| 32초 | 239자 |
| **35초 (상한)** | **261자** |

안전 여유를 두고 **240자 이내**로 쓴다.

### 대본 예산

- **컷 수 14~17개** (그 이상이면 거의 반드시 초과한다)
- **컷당 12~14자**
- 오프닝 카드(`title.card`)도 **읽어주는 문장이라 길이에 포함된다** — 24자 이내
- 밈 컷(PUNCH) 3~4개 포함

### 게이트 — script 단계에서 반드시 확인

`step='script'` 응답의 `grammar.estimated_sec` 를 본다.

- **34 초과면 렌더로 넘어가지 말고 컷을 줄여 다시 제출한다.**
- 서버 추정치는 실제보다 약 2~3% 높게 나온다(72.8 추정 → 70.9 실제). 그래도 여유를 남긴다.
- `stats.groups` 가 18 이상이면 이미 길다는 신호다.

### 길이가 넘칠 때 먼저 덜어낼 것

기사를 빠짐없이 담으려는 충동이 길이를 늘린다. 사실관계는 지키되 **부연 설명부터** 덜어낸다.

1. 용어 뜻풀이 컷 (예: "내선일체는 ~라는 뜻임")
2. 어원·배경 설명 컷 (예: "대정은 일왕 다이쇼에서 따옴")
3. 같은 취지를 두 번 말하는 컷
4. 인용문 중 핵심이 아닌 것

`repeated_image_groups` 가 많이 나오면 **그림보다 설명 컷이 많다는 뜻**이니 줄여야 한다는 신호다.

---

## ★숫자를 자막에 그대로 쓰면 낭독이 틀린다 — 여섯 가지 (2026-08-22 재확인)

서버는 `groups[].text` **한 값으로 자막과 낭독을 겸한다.** 화면에 숫자를 쓰면 그 숫자가
그대로 TTS 로 가는데 **여섯 경우를 잘못 읽는다.** 오류가 안 나서 완성본에 그대로 실린다.

| 자막에 쓰고 싶은 것 | 서버가 읽는 소리 | 맞는 발음 |
|---|---|---|
| `112` · `119` | 백십이 · 백십구 | 일일이 · 일일구 |
| `20대` · `30대` | 스무대 · 서른대 | 이십 대 · 삼십 대 |
| `5번` | 오번 | 다섯 번 |
| `2마리` | 이마리 | 두 마리 |
| `3번째` | 삼번째 | 세 번째 |
| `3분의 1` | 세분의 일 | 삼분의 일 |

★**한글로 적으면 서버가 한 글자도 손대지 않는다.** 그래서 이렇게 푼다 —

1. 대본에는 **낭독형(한글)** 을 적는다 — `결국 일일이에 신고했음`
2. `subtitle` 이 끝나면 자막 파일만 **표기형(숫자)** 으로 되돌린다

```
python patch_read_ko.py <workdir> --from read_map.json      # {"일일이": "112", ...}
python patch_read_ko.py <workdir> --map "두 마리=2마리" --dry
```

`sub.ass` 와 `timing.json` 을 함께 고친다. **짝이 하나라도 0건 적중이면 실패로 끝내고
원본으로 되돌린다** — 반쪽 적용이 제일 위험하다(낭독형 한글이 그대로 화면에 나간다).

- **천 단위 쉼표도 이걸로 푼다.** 자막에 `,` 를 쓰면 반려되니 대본엔 `천이백` 으로 적고
  나중에 `1,200` 으로 되돌린다
- 줄나눔이 **낭독형(긴 쪽) 폭으로** 계산돼 있어 숫자로 되돌리면 늘 짧아진다 — 안전한 방향이다.
  거꾸로 표기형이 더 길면 스크립트가 경고한다

## 서버 게이트 — 지금 이런 상태다 (2026-08-22 전수 재측정)

`SERVER-ISSUES-볼케이노-20260814.md` 11건 중 **6건 해결 · 5건 잔존**이다.
그 문서는 **볼케이노 폴더 밖 한 단계 위**에 있다([[volcano-server-issues-doc-location]]).

| 남은 것 | 어떻게 넘기나 |
|---|---|
| #3 TTS 숫자 오독 · #4 자막·낭독 분리 · #7 자막 쉼표 | 위 `patch_read_ko.py` 하나로 전부 우회된다 |
| #6 어두운 실사를 그림으로 반려 | 밤·실루엣을 빼고 질감을 넣어 그 슬롯만 다시 굽는다 |
| #11 마지막 문장 미종결 경고 | **막지 않는다. 왕복하지 마라** — 기준이 공개돼 있지 않다 |

- **#10 지역 판정은 이제 반려가 아니라 경고다.** 키워드 수까지 알려주고(`해외5/국내1`),
  부분문자열로 세어 「20대만」을 대만으로 읽는다는 주의까지 붙는다.
  **제출한 판정이 맞으면 그대로 두면 된다**
- 고쳐진 것: #1 `measure_sfx`·#2 팩 sha·#5 헤드라인 숫자 오탐·#8 `out/`·#9 나레 끝 정적
- 무과금 재현법 — `script` 까지만 불러 게이트를 보고, `voice` 를 한 번 불러
  `jobs[].body.text` 를 자막과 대조한다. 이미지·합성을 안 돌려도 여기까지 온다

## 그 밖의 고정 사항

- **★실사 사진이 기본이다. 없는 장면만 생성한다** (사장님 지시 2026-08-20).
  "실존 인물 얼굴만 실사" 는 낡은 규칙이니 그렇게 읽지 마라. 뇌전구는 사진 한 장이
  길어야 2초라 그 정도 인용은 무리가 없고, **Content ID 는 정지 이미지에 안 걸린다**
  (오디오·영상 지문 방식이다. 못 쓰는 건 [영상]이지 사진이 아니다).
  - `og:image` 하나만 보지 말고 **본문 `<img>` 전부·첨부 이미지 전부**를 후보로 긁는다.
    커뮤니티 글이면 캡쳐·풍경사진도 후보다
  - 후보는 썸네일 시트로 뽑아 **눈으로 고른다.** 고른 슬롯은 이미지 생성 job 에서 빼고
    파일만 직접 넣으면 된다
  - 규격(1184×880)에 맞춰 크롭한다. 세로 사진은 좌우 검은 띠를 잘라내고,
    크롭 중심은 자동값 말고 눈으로 확인한다
  - **덤** — 생성 장수가 줄어 `repeated_image_groups` 를 비우기가 쉬워지고,
    실사 비중이 높으면 실사·생성이 섞여 톤이 튀는 문제도 사라진다(한두 장만 끼는 게 제일 어색하다)
  - **노출이 짧다고 저작권 상태가 달라지진 않는다.** 커뮤니티 캡쳐의
    **닉네임·프로필 사진·커뮤니티 로고는 가린다** — 특정되면 2초든 20초든 같다.
    캡쳐를 통째로 붙이는 것보다 문구만 가져와 다시 조판하는 쪽이 화면도 깔끔하다
  - 납품할 때 **실사 몇 장 / 생성 몇 장**인지 밝힌다. 사장님이 과한지 보고 조절하신다
- **`prompts` 단계에서 서버가 박는 로케일을 확인한다.** 서버는 `region` 하나로 **모든 슬롯**에
  `in <나라>` 를 일괄 주입한다. 장면별 무대가 다르면(예: 서울/일제강점기 조선 vs 중국)
  슬롯마다 로케일을 직접 고쳐야 한다. 규격 문구(읽히는 글자·브랜드 금지)는 절대 건드리지 않는다.
- 이미지는 프롬프트 기준으로 `cache/img/<key>.png` 에 캐시된다.
  재편집 시 **같은 프롬프트 문자열을 그대로 쓰면 재생성 비용이 들지 않는다.**
- **★컷마다 다른 그림을 써라.** `script` 응답의 `repeated_image_groups` 가 **비어 있어야 한다.**
  같은 그림을 연달아 쓰면 서버가 줌을 확 바꿔 붙이는데, **화면은 그대로인 채 배율만 튀어**
  시청자가 피로해진다(사장님 지적 2026-08-20). 컷이 15개면 밈 3개를 뺀 12장을 다 만든다.
  줄일 거면 줌을 바꾸지 말아야 하는데 그건 서버가 정하므로, **그림을 늘리는 쪽이 답이다.**
  기존 슬롯 번호가 밀려도 **프롬프트 문자열만 그대로면 캐시가 그대로 맞는다** — 새로 만드는
  몇 장만 값을 치른다.
- **`no people` 을 프롬프트에 넣지 마라.** 서버가 넣어 주는 `real people` 을 덮어써서
  빈 건물·빈 회의실·빈 연단만 나온다(2026-08-20 실기, 7장 전부 다시 구웠다).
  얼굴이 걱정되면 사람을 빼지 말고 **`shot from behind so no faces are visible`** 로 가린다.
- **`image_facts` 의 cx/cy 는 자주 틀린다.** 실측 12장 중 4장이 어긋났다(연단이 가운데인데
  cx 0.205, 손이 화면을 꽉 채우는데 cy 0.14). **시트를 보고 눈으로 고쳐서** 넣는다.
- 실행은 반드시 `runner/volcano_run.py` 배급 실행기로 한다. 직접 짜지 않는다.
- 파이썬: `C:/Users/kjg34/AppData/Local/Programs/Python/Python312/python.EXE`
- 자산 다운로드는 `curl` 로 한다 (파이썬 urllib 는 워커에서 403).
  ★PowerShell 에서 `curl` 은 `Invoke-WebRequest` 별칭이라 `-sSL` 에서 죽는다 — 셸에서는 **`curl.exe`** 로 부른다.

## ★납품 — 완성 mp4 는 지정 폴더로 내보낸다

렌더가 끝나면 **반드시** 마지막에 한 번 돌린다. 사장님은 `work_*` 폴더를 뒤지지 않으신다.

```
python deliver.py <workdir> "<유튜브 제목>"
```

## ★★제목 끝에 카테고리를 붙인다 — 예외 없이 전부

사장님이 채널을 여럿 굴리셔서 완성본 폴더에서 구분이 안 된다 (지시 2026-08-21).
**앞으로 내보내는 모든 것**의 `deliver.py` 제목 **맨 끝**에 `(카테고리)` 를 넣는다.
한국어든 일본어든, 어느 채널이든 마찬가지다.

```
python deliver.py <workdir> "20円上げただけで客が1割消えた丸亀製麺のいま (경제)"
python deliver.py <workdir> "자진 퇴사해도 실업급여 해외는 이렇게 막고 있었다 (경제)"
```

쓰는 말: `(경제)` `(정치)` `(사회)` `(국제)` `(축구)` `(골프)` `(인물)` `(칩칩)`
— 소재의 갈래로 적는다. 애매하면 사장님께 여쭙지 말고 가장 가까운 것을 골라 밝힌다.

- **화면·자막·오프닝 카드에는 넣지 않는다.** 파일명 전용이다
- 서버 `title.youtube` 에도 넣지 마라 — **괄호는 반려된다**(실측 2026-08-21).
  `deliver.py` 에 넘길 때만 붙인다
- 중복 판정은 이 꼬리표를 떼고 한다 (`ref_econ/seed_seen.py`)

- 나가는 곳: `C:\Users\kjg34\Desktop\볼케이노 완성본\`
- 파일명: `YYYYMMDD_<유튜브 제목>.mp4` (날짜는 mp4 수정 시각, 금지 문자는 공백으로 치환)
- `out/` 의 가장 높은 `v###` 을 고른다. 같은 이름이 있고 크기가 다르면 `_2`·`_3` 을 붙인다.
- **작업 폴더(`work_*`)는 지우지 않는다.** `cache/img`·`cache/tts` 가 거기 있어야 재편집이 공짜다.
- 답변 맨 끝에는 **납품 경로**를 코드블록으로 준다 (work 폴더 경로 말고).

---
