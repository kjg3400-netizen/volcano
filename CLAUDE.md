# ★사장님 채널 지도 (2026-08-21 확인)

| 갈래 | 한국 | 일본 | 소재 자동화 |
|---|---|---|---|
| 경제 | **숏단지** `UCBbTfTx1XafCl6JTJ2KgNcg` | **숏피드(ショーフィ)** `UClyn9gKhLeA4OOJclpjGm1Q` | ✅ `ref_econ` · `ref_jpecon` |
| 커뮤 | **방탐** (뇌전구커뮤) | **능구렁이** | ✅ `ref_comm` · `ref_jpcomm` |
| 축구 | **짹짹 (JJACK_J)** 구독 6,550 | **神ショーツ** | ❌ |
| 춤·스토리 | **칩칩** `UCMkYFJ7J5ELuhQ-lWKGMlXA` 구독 2,950 | **ランカー** | ❌ |
| 골프 | **짧뷰** `@짧뷰노노` 구독 1,840 | — | ❌ |

## ★두 부류는 소재 성격이 근본부터 다르다

- **글 채널** (경제·커뮤) — 네이버·야후·커뮤에 **공개된 반응 수치**가 있다.
  그래서 긁어서 재고 점수를 매길 수 있다. 자동화가 되는 이유가 이것이다.
- **클립 채널** (축구·골프·춤) — 소재가 **남의 영상**이다. 반응 수치를 긁을 데가 없고,
  `yt-dlp` 로는 쇼츠 발굴이 막혀 있다([[ytdlp-shorts-discovery-blocked]]) —
  검색·해시태그가 전부 롱폼만 준다. **되는 건 채널 `/shorts` 카탈로그뿐이다.**

  → 그래서 클립 채널의 발굴기는 **소재를 길어 오는 채널 목록**이 있어야 만들 수 있다.
    사장님이 평소 어디서 클립을 구해 오시는지가 입력값이다. 그 목록만 있으면
    매일 카탈로그를 받아 **조회 급등**을 잡아내는 방식이 된다.
  → 보조 수단으로 `ref_comm --only` 갈래 필터가 있다(커뮤에 뜬 화제 클립을 잡는다).

## 채널별 성과 (2026-08-21)

| 채널 | 구독 | 조회 중앙 | 최고 |
|---|--:|--:|--:|
| 짹짹 | 6,550 | 31,000 | 3,700,000 |
| 칩칩 | 2,950 | 22,000 | 2,100,000 |
| 짧뷰 | 1,840 | 35,000 | 6,600,000 |
| 숏단지 | 20 | 4,199 | 16,354 |
| 숏피드 | 12 | 5,643 | 7,948 |

---

# 볼케이노 채널 작업 규약

## 영상 길이 상한 — 35초 (절대)

**35초를 넘기면 안 된다.** 30초 언저리가 이상적이다.

볼케이노 MCP 서버는 줄 단위 검증(한 줄 5어절·화면 폭)만 하고 **총 길이는 검증하지 않는다.**
길이는 전적으로 대본 작성자가 정하므로, 아래 게이트를 직접 통과시켜야 한다.

### 계산식

실측 7회차에서 **총 길이 ≈ 총 글자수(공백 제외, 오프닝 카드 포함) × 0.134초** 로 일정하다(편차 ±2%).

| 목표 | 총 글자수(카드 포함) |
|---|---|
| 30초 | 224자 |
| 32초 | 239자 |
| **35초 (상한)** | **261자** |

안전 여유를 두고 **240자 이내**로 쓴다.

### 대본 예산

- **컷 수 14~17개** (그 이상이면 거의 반드시 초과한다)
- **컷당 12~14자**
- 오프닝 카드(`title.card`)도 **읽어주는 문장이라 길이에 포함된다** — 24자 이내
- 밈 컷(PUNCH) 3~4개 포함

### 게이트 — script 단계에서 반드시 확인

`step='script'` 응답의 `grammar.estimated_sec` 를 본다.

- **34 초과면 렌더로 넘어가지 말고 컷을 줄여 다시 제출한다.**
- 서버 추정치는 실제보다 약 2~3% 높게 나온다(72.8 추정 → 70.9 실제). 그래도 여유를 남긴다.
- `stats.groups` 가 18 이상이면 이미 길다는 신호다.

### 길이가 넘칠 때 먼저 덜어낼 것

기사를 빠짐없이 담으려는 충동이 길이를 늘린다. 사실관계는 지키되 **부연 설명부터** 덜어낸다.

1. 용어 뜻풀이 컷 (예: "내선일체는 ~라는 뜻임")
2. 어원·배경 설명 컷 (예: "대정은 일왕 다이쇼에서 따옴")
3. 같은 취지를 두 번 말하는 컷
4. 인용문 중 핵심이 아닌 것

`repeated_image_groups` 가 많이 나오면 **그림보다 설명 컷이 많다는 뜻**이니 줄여야 한다는 신호다.

---

## 그 밖의 고정 사항

- **★실사 사진이 기본이다. 없는 장면만 생성한다** (사장님 지시 2026-08-20).
  "실존 인물 얼굴만 실사" 는 낡은 규칙이니 그렇게 읽지 마라. 뇌전구는 사진 한 장이
  길어야 2초라 그 정도 인용은 무리가 없고, **Content ID 는 정지 이미지에 안 걸린다**
  (오디오·영상 지문 방식이다. 못 쓰는 건 [영상]이지 사진이 아니다).
  - `og:image` 하나만 보지 말고 **본문 `<img>` 전부·첨부 이미지 전부**를 후보로 긁는다.
    커뮤니티 글이면 캡쳐·풍경사진도 후보다
  - 후보는 썸네일 시트로 뽑아 **눈으로 고른다.** 고른 슬롯은 이미지 생성 job 에서 빼고
    파일만 직접 넣으면 된다
  - 규격(1184×880)에 맞춰 크롭한다. 세로 사진은 좌우 검은 띠를 잘라내고,
    크롭 중심은 자동값 말고 눈으로 확인한다
  - **덤** — 생성 장수가 줄어 `repeated_image_groups` 를 비우기가 쉬워지고,
    실사 비중이 높으면 실사·생성이 섞여 톤이 튀는 문제도 사라진다(한두 장만 끼는 게 제일 어색하다)
  - **노출이 짧다고 저작권 상태가 달라지진 않는다.** 커뮤니티 캡쳐의
    **닉네임·프로필 사진·커뮤니티 로고는 가린다** — 특정되면 2초든 20초든 같다.
    캡쳐를 통째로 붙이는 것보다 문구만 가져와 다시 조판하는 쪽이 화면도 깔끔하다
  - 납품할 때 **실사 몇 장 / 생성 몇 장**인지 밝힌다. 사장님이 과한지 보고 조절하신다
- **`prompts` 단계에서 서버가 박는 로케일을 확인한다.** 서버는 `region` 하나로 **모든 슬롯**에
  `in <나라>` 를 일괄 주입한다. 장면별 무대가 다르면(예: 서울/일제강점기 조선 vs 중국)
  슬롯마다 로케일을 직접 고쳐야 한다. 규격 문구(읽히는 글자·브랜드 금지)는 절대 건드리지 않는다.
- 이미지는 프롬프트 기준으로 `cache/img/<key>.png` 에 캐시된다.
  재편집 시 **같은 프롬프트 문자열을 그대로 쓰면 재생성 비용이 들지 않는다.**
- **★컷마다 다른 그림을 써라.** `script` 응답의 `repeated_image_groups` 가 **비어 있어야 한다.**
  같은 그림을 연달아 쓰면 서버가 줌을 확 바꿔 붙이는데, **화면은 그대로인 채 배율만 튀어**
  시청자가 피로해진다(사장님 지적 2026-08-20). 컷이 15개면 밈 3개를 뺀 12장을 다 만든다.
  줄일 거면 줌을 바꾸지 말아야 하는데 그건 서버가 정하므로, **그림을 늘리는 쪽이 답이다.**
  기존 슬롯 번호가 밀려도 **프롬프트 문자열만 그대로면 캐시가 그대로 맞는다** — 새로 만드는
  몇 장만 값을 치른다.
- **`no people` 을 프롬프트에 넣지 마라.** 서버가 넣어 주는 `real people` 을 덮어써서
  빈 건물·빈 회의실·빈 연단만 나온다(2026-08-20 실기, 7장 전부 다시 구웠다).
  얼굴이 걱정되면 사람을 빼지 말고 **`shot from behind so no faces are visible`** 로 가린다.
- **`image_facts` 의 cx/cy 는 자주 틀린다.** 실측 12장 중 4장이 어긋났다(연단이 가운데인데
  cx 0.205, 손이 화면을 꽉 채우는데 cy 0.14). **시트를 보고 눈으로 고쳐서** 넣는다.
- 실행은 반드시 `runner/volcano_run.py` 배급 실행기로 한다. 직접 짜지 않는다.
- 파이썬: `C:/Users/kjg34/AppData/Local/Programs/Python/Python312/python.EXE`
- 자산 다운로드는 `curl` 로 한다 (파이썬 urllib 는 워커에서 403).
  ★PowerShell 에서 `curl` 은 `Invoke-WebRequest` 별칭이라 `-sSL` 에서 죽는다 — 셸에서는 **`curl.exe`** 로 부른다.

## ★납품 — 완성 mp4 는 지정 폴더로 내보낸다

렌더가 끝나면 **반드시** 마지막에 한 번 돌린다. 사장님은 `work_*` 폴더를 뒤지지 않으신다.

```
python deliver.py <workdir> "<유튜브 제목>"
```

## ★★제목 끝에 카테고리를 붙인다 — 예외 없이 전부

사장님이 채널을 여럿 굴리셔서 완성본 폴더에서 구분이 안 된다 (지시 2026-08-21).
**앞으로 내보내는 모든 것**의 `deliver.py` 제목 **맨 끝**에 `(카테고리)` 를 넣는다.
한국어든 일본어든, 어느 채널이든 마찬가지다.

```
python deliver.py <workdir> "20円上げただけで客が1割消えた丸亀製麺のいま (경제)"
python deliver.py <workdir> "자진 퇴사해도 실업급여 해외는 이렇게 막고 있었다 (경제)"
```

쓰는 말: `(경제)` `(정치)` `(사회)` `(국제)` `(축구)` `(골프)` `(인물)` `(칩칩)`
— 소재의 갈래로 적는다. 애매하면 사장님께 여쭙지 말고 가장 가까운 것을 골라 밝힌다.

- **화면·자막·오프닝 카드에는 넣지 않는다.** 파일명 전용이다
- 서버 `title.youtube` 에도 넣지 마라 — **괄호는 반려된다**(실측 2026-08-21).
  `deliver.py` 에 넘길 때만 붙인다
- 중복 판정은 이 꼬리표를 떼고 한다 (`ref_econ/seed_seen.py`)

- 나가는 곳: `C:\Users\kjg34\Desktop\볼케이노 완성본\`
- 파일명: `YYYYMMDD_<유튜브 제목>.mp4` (날짜는 mp4 수정 시각, 금지 문자는 공백으로 치환)
- `out/` 의 가장 높은 `v###` 을 고른다. 같은 이름이 있고 크기가 다르면 `_2`·`_3` 을 붙인다.
- **작업 폴더(`work_*`)는 지우지 않는다.** `cache/img`·`cache/tts` 가 거기 있어야 재편집이 공짜다.
- 답변 맨 끝에는 **납품 경로**를 코드블록으로 준다 (work 폴더 경로 말고).

---

# 경제 소재 발굴 — 네이버에서 '실제로 반응 붙은 것'만 긁어 온다

## 부르는 법

> `경제 소재 뽑아 줘` · `오늘 뭐 만들까` · `<번호>번으로 가`

사장님이 손으로 하시던 **뒤져서 될 만한 것 고르기**를 대신한다.
되묻지 말고 돌린 뒤 시트를 보여 드리고, **뇌전구로 뒤집을 만한 것 3~5개를 추려 추천**한다.

```
python ref_econ/hunt.py                  # 경제 300여 건 → 반응 실측 → 점수순 (약 80초)
python ref_econ/brief.py --pick 3        # 고른 사건의 본문·사진·시트 준비
python ref_econ/seed_seen.py             # 납품 이력을 중복목록에 반영 (회차마다 한 번)
python ref_econ/learn.py                 # 채널 실적 다시 학습 (회차 쌓이면)
```

**매일 06 / 13 / 20시에 저절로 돈다** (윈도우 작업 스케줄러, 2026-08-21 등록).
업로드는 **08 / 17시** — 뇌전구형은 하루 2편이다 (사장님 지시 2026-08-21).
헌터는 3회 그대로 둔다 — 시트가 최신일수록 좋고, 20시치는 다음날 아침 몫이다. 결과는 늘 `ref_econ/out/_최신시트.md`.
사장님이 "오늘 뭐 만들까" 하시면 **새로 돌리지 말고 그 파일부터 열어 봐라** — 이미 있다.

시작 전에 `ref_econ/econ_spec.md` 를 읽는다. 아래는 요약이다.

## ★네이버는 오류 없이 조용히 틀린다 — 네 군데

| 함정 | 증상 | 대응 |
|---|---|---|
| 랭킹의 `sectionId` | **무시된다.** 101·100·생략이 전부 같은 375건 | 후보 풀은 `SECTION_ARTICLE_LIST?sid=` 로만. 랭킹은 가산점 전용 |
| 인코딩 | 랭킹(`.naver`)은 **EUC-KR**, 섹션은 UTF-8 | utf-8 로 못박으면 제목이 깨지고 경과시간이 통째로 빈다 |
| 감정반응 | `화나요`·`좋아요` 는 **없어졌다** (쏠쏠정보·흥미진진·공감백배·분석탁월·후속강추 5종) | '화나요 비율'로 논쟁도를 재려 하지 마라 — 항상 0 이다 |
| `/cluster/` | 사건 묶음이 아니라 **주제 피드**다 (한 묶음에 132건) | 믿고 묶으면 엉뚱한 기사·사진이 딸려 온다. 제목 유사도로 묶는다 |

## 고르는 기준

```
최종 점수 = 네이버 반응 점수 × 채널 실적 배수
```

네이버 쪽은 실측이 주(主)다 — 댓글수, 감정반응, 몇 개 매체가 받아썼나,
전 섹션 랭킹에 들었나, 갓 올라왔는데 벌써 뜨거운가(속도).
`매체` 열이 크면 큰 사건이고, `정보%`가 높으면 정보형이다.

## ★네이버 반응만 믿으면 틀린다 — 채널 실적을 곱한다

네이버에서 반응이 좋아도 **이 채널에서 안 먹히는 꼴**이 있다. 숏단지 15편 실측(2026-08-20):

| 꼴 | 중앙 조회 | 배수 |
|---|--:|--:|
| 한국 vs 해외 | 16,354 | ×1.60 |
| 인물·거액 | 8,102 | ×1.48 |
| 증시·기업 | 4,024 | ×0.99 |
| 제도·거시 | 2,409 | ×0.90 |
| 사연 | 2,658 | ×0.86 |

**첫 추천이 이걸 몰라 틀렸다** — 전세 안심신탁을 `정보% 95` 만 보고 1순위로 밀었는데
제도·거시는 이 채널 최하위 꼴이었다. 잘 되는 편은 **주체가 뚜렷하고 금액이 크다**
(`중국이 52척 싹쓸이`, `455억 번 회장님`). 안 되는 편은 주체가 흐릿하다.

- 배수는 `learn.py` 가 채널을 받아 계산한다. **회차가 쌓이면 다시 돌려라** — 과적합 방지로
  1.0 쪽으로 당겨 둔 축소가 풀리면서 날카로워진다
- 꼴 분류(`shapes.py`)는 **순서가 결과를 바꾼다.** 두루뭉술한 낱말(글로벌·세계)을
  넣지 마라 — 그냥 증시 기사가 ×1.6 을 받아 1위로 올라온다

## ★★중국 소재가 이 채널 최고 꼴이다 — 시트 맨 위 칸을 먼저 봐라

사장님이 먼저 알아채신 것을 실측으로 확인했다 (2026-08-21, 두 채널 21편 전수).

| | 중국 편 | 나머지 | 배수 |
|---|--:|--:|--:|
| 숏단지(한국) | **15,000** (2편) | 4,350 (14편) | **3.45배** |
| 숏피드(일본) | **9,500** (3편) | 3,750 (2편) | **2.53배** |

**양쪽 채널 1·2위가 전부 중국 편이다.** 숏단지는 중국 없는 14편 최고(8,500)를
중국 2편이 둘 다 넘겼다.

### ★낱말이 아니라 구도다 — 이걸 헷갈리면 시트가 오염된다

`「日本製」なのに中国産が入る` 는 중국이 나오는데 **1,200 으로 최저권**이었다.
중국이 배경일 뿐 주제가 원산지 표시 제도라서다. 터진 편은 전부
**자리를 뺏고 뺏기는 구도**다 — `LNG선마저 중국이 싹쓸이` · `삼성을 제치고 중국산 D램`
· `中国客が半減したのに過去最多`.

그래서 `shapes.py` 가 **중국 낱말 AND 대결 낱말** 두 조건으로 건다. 한쪽만으로는 안 된다.
`수출`·`1위`·`위협` 같은 넓은 낱말을 넣으면 무역·ETF·군사 기사가 통째로 딸려 온다(실측으로 뺐다).

### 시트 보는 법

시트 맨 위 **`★중국 소재`** 칸이 이것이다. `◎맞대결` 이 위 구도이고 `○관련` 은
중국이 나오지만 구도는 아닌 것 — **○는 제목을 읽고 직접 고르셔야 한다.**

- **점수 순위로 밀지 않는다.** 배수 상한이 1.6 이라 `네이버 437 × 0.99` 가
  `네이버 172 × 1.6` 을 이긴다. 물량 많은 증시 기사가 상위를 도배하므로
  **별도 칸으로 뽑는 게 답이다** (실측 raw 배수는 4.05 인데 1.6 으로 잘려 있다)
- 폰 알림에도 이 칸이 맨 위에 온다 (`notify_hunt.py` 의 `china_block`)

### 섹션을 101,105,104 로 넓혔다

경제 섹션만 보면 상위 20개 중 `한국vs해외` 가 **3개**뿐이고 그것도 9·18·19위였다
(증시·기업이 11개로 도배). 세계 섹션은 13개 중 11개를 준다 —
다만 **태반이 트럼프·이란·북핵이라 못 쓴다.** 그래서 `MILITARY_WORDS` 로 군사·외교를 뺀다.
`daily.cmd` 가 `--sid 101,105,104` 로 돌고 실측이 1.5분 → 5분으로 늘었다(예약이라 무방).

**시세·공시 기사는 자동으로 깎이지만 완벽하진 않다.** 추천 전에 제목을 직접 읽고
"30초 안에 뒤집을 알맹이가 있나"를 판단한다 — 이건 스크립트가 못 한다.
**프록시 지표만 보고 답하지 마라. 채널 실적은 `yt-dlp` 로 언제든 받아볼 수 있다.**

## 소재 준비 뒤

`brief.py` 가 `work_econ_<날짜>_<기사id>/` 에 `body.txt` · `real/` · `_sheet_real.jpg` 를 만든다.
**시트는 반드시 눈으로 본다** (규격 1184×880 에 못 미치는 사진이 섞인다).
사진이 모자라면 `--with-siblings` 로 같은 사건 다른 매체 것까지 긁는다.
이후는 기존 뇌전구 파이프라인 그대로 — 240자 이내, 컷 14~17개, 35초 상한.

## 재사용 스크립트 — 전부 `ref_econ/` 에 있다

`hunt.py` (발굴·실측·점수 — `--from-raw` 로 캐시 재사용, `--sid` 로 섹션 추가,
`--group-th` 로 사건 묶는 세기, `--no-fit` 로 채널 배수 끄기) · `brief.py` (소재 준비)
· `learn.py` (채널 실적 학습 → `channel_fit.json`) · `shapes.py` (꼴 분류, hunt·learn 공용)
· `seed_seen.py` (중복목록) · `daily.cmd` (예약 실행 래퍼 — ★ASCII 만 쓴다)
· `econ_spec.md` (실측 기록) · 결과는 `ref_econ/out/`
· 채널 진단 스크립트는 `work_ch_sudanjip/`

---

# 일본 경제 소재 발굴 — 야후에서 긁되 대전제로 거른다

## 부르는 법

> `일본 경제 소재 뽑아 줘` · `쇼피드 뭐 만들까`

```
python ref_jpecon/hunt.py                 # Yahoo 経済 → 반응 실측 → 점수순 (약 20초)
python ref_jpecon/hunt.py --show-unsafe   # 걸러진 것도 같이 본다
python ref_jpecon/brief.py --pick 1       # 고른 사건의 기사·사진·시트 준비
python ref_jpecon/seed_seen.py            # 중복목록 갱신
```

**매일 06:05 / 13:05 / 20:05 에 저절로 돈다** (2026-08-21 등록).
업로드 08 / 17시 기준이고(하루 2편), 한국판(정각)보다 5분 뒤로 밀어 겹치지 않게 했다.
결과는 늘 `ref_jpecon/out/_최신시트.md` — **새로 돌리지 말고 그 파일부터 열어 봐라.**

시작 전에 `ref_jpecon/jpecon_spec.md` 를 읽는다. 채널은 **ショーフィ(쇼피드)**
`UClyn9gKhLeA4OOJclpjGm1Q`.

## 야후는 네이버보다 낫다

**랭킹이 카테고리별로 진짜 갈린다** — 한국판에서 제일 고생한 함정이 여기엔 없다.
코멘트 수는 기사 페이지 JSON 의 `totalCount`, 추가 신호로 はてなブックマーク가 있다.
다만 **물량이 얇다**(한 번에 66건, 한국은 620건).

## ★대전제 안전 판정 — 이게 한국판에 없는 부분이다

일본 경제 뉴스는 **구조적으로 일본의 어려움 이야기가 많다.** 반응만 보고 고르면
자동으로 "일본 경제가 어렵다"가 되어 대전제(비난 금지·시청자가 일본인)를 어긴다.

`-0.5` 이하면 목록에서 빼고 시트 맨 아래 **"만들지 마라"** 칸에 따로 적는다.
**그 칸을 반드시 읽어라 — 반응 큰 게 거기 있다.**

| 깎는 것 | 올리는 것 |
|---|---|
| 정부 **+** 실책어 (−0.85) | 해외가 줄었는데 일본은 늘었다 (+0.55) |
| 국민부담(증세·값인상) (−0.40) | 역설 `のに`·`にもかかわらず` (+0.25) |
| 일본이 진 대비 (−0.70) | 의문형 `ワケ`·`理由`·`なぜ` (+0.15) |
| 실명 개인의 불행 (−0.55) | |

- **정부가 주어라는 것만으로 깎지 마라.** 막는 건 비난이지 정부 이야기가 아니다.
  `霞が関が国産AIを導入`(코멘트 1,017)이 `国会` 한 낱말로 밀렸던 오탐을 고쳤다
- 실명 개인 불행은 대전제와 별개지만 이 채널 소재가 아니다

## ★중국 소재 — 여기도 최고 꼴이지만 **조건이 정반대다**

쇼피드 5편 실측에서 1·2위가 둘 다 중국 편이었다 (중앙 9,500 vs 3,750 = 2.53배).
그런데 한국판처럼 '뺏고 뺏기는 구도'를 쓰면 **대전제 위반**이 된다 — 일본이 지는 이야기다.

터진 두 편은 **일본이 이긴** 구도다:

```
26,000  中国人観光客が激減した「のに」過去最多の344万人
 9,500  中国客が半減して5210億円消えた「のに」逆に売上爆増
 1,200  「日本製」なのに中国産が入る          ← 최저권. 일본이 이긴 이야기가 아니다
```

- **`のに` 하나로 최고 등급을 주면 안 된다.** 1,200 짜리에도 `のに` 가 있다.
  `JP_WIN`(過去最多·爆増·最高益…)이 **함께** 있어야 한다
- **일본이 중국에 밀린 표현은 아예 뺀다** (`CN_LOSE`). safety() 만으로는 안 걸린다 —
  `中国には強気な高市首相がロシアには手も足も出ない…屈辱的な` 가 안전 +0.00 으로
  통과해 후보에 올랐다(실기 2026-08-21). `屈辱`·`手も足も出な`·`奪われ` 까지 잡는다

## ★한국판 배수를 가져다 쓰지 마라

숏단지 최고 꼴이 `한국이 중국에 밀린` 이야기인데, 일본판에서 `日本が負けた` 를 하면
대전제 위반이다. **정반대다.** 쇼피드 상위 두 편은 둘 다
`외국이 줄었는데 일본은 늘었다` — 역설이면서 일본이 이긴 쪽이다.

`learn.py` 는 성숙한 편 5개가 필요한데 아직 3편이라 **배수는 전부 ×1.0** 이고
안전 판정만 돈다. 편이 쌓이면 그때 학습한다.

## 재사용 스크립트 — 전부 `ref_jpecon/` 에 있다

`hunt.py` (발굴·실측·안전판정) · `shapes_jp.py` (꼴 분류 + 대전제 안전도)
· `brief.py` (소재 준비 — ★스톡 크레딧을 경고한다) · `learn.py` (채널 실적 학습)
· `seed_seen.py` (중복목록 — **가나로** 한국 회차와 가른다) · `daily.cmd` (예약 래퍼, ASCII 만)
· `jpecon_spec.md` (실측 기록) · 결과는 `ref_jpecon/out/` · 채널 진단은 `work_ch_shortfi/`

★**야후 기사 사진은 `newsatcl-pctr` 호스트만 쓴다.** `news-pctr`(사이드바 추천)·
`s.yimg.jp`(UI 아이콘)를 안 거르면 무관한 사진이 딸려 온다 — 실측 6장 중 5장이 그랬다.
그리고 **대표사진이 스톡(`stock.adobe.com`)인 경우가 흔하다** — 매체가 산 것이라 못 쓴다.

---

# 일본 커뮤형 소재 발굴 — 커뮤 9계열 17곳에서 긁어 온다

## 부르는 법

> `일본 커뮤 소재 뽑아 줘` · `일본 커뮤에 뭐 떴냐` · `<번호>번으로 가`

```
python ref_jpcomm/hunt.py                     # 17곳 2,500여 건 → 점수순 (약 40초)
python ref_jpcomm/hunt.py --no-news           # 교차확인 끄고 빠르게 (4초)
python ref_jpcomm/hunt.py --show-blocked      # 절대금지로 뺀 것도 본다
python ref_jpcomm/hunt.py --only 生活・マナー論争,逆説・雑学
```

시작 전에 `ref_jpcomm/jpcomm_spec.md` 를 읽는다. 채널은 **ショーフィ(쇼피드)** 로
`ref_jpecon` 과 같고, **대전제(비난 금지·시청자가 일본인)가 그대로 적용된다.**

## ★일본엔 펨코가 없다 — 그래서 여러 곳을 나란히 긁는다

한국은 `fmkorea.com/best` 하나가 전 게시판을 모아 주지만 일본은 그런 곳이 없다.
판이 성별·계층별로 갈라져 있어 **한 곳만 보면 그 판 목소리만 듣는다.**

**뚫림 17곳** — ガルちゃん(인기·신착) · 発言小町(일간·주간) · 5ch(ニュース速報+·芸スポ+·嫌儲)
· はてブ(총합·생활·학습) · Togetter(주목·톱) · ヤフコメ(총합·국내·생활) · ハム速 · 痛いニュース.
전부 `requests` 로 열리고 2,500여 건이 3~4초다.

- **ガルちゃん 인기 랭킹은 `/ranking/` 이 아니라 루트(`/`)** 다. `/ranking/` 은 403
- **5ch 판 서버는 `headline.5ch.io/ikioi/` 에서 찾는다.** `bbsmenu.html` 은 이제 포털이라 안 나온다
- 막힌 곳 — ママスタ(JS 챌린지) · ikioi2ch(고사) · Togetter `/ranking`(JS)

## ★조용히 틀리는 곳 — 여섯

| 함정 | 증상 | 대응 |
|---|---|---|
| 인코딩 3종 | 5ch=**cp932** · 痛いニュース=**euc-jp** · 나머지 utf-8 | utf-8 로 못박으면 제목만 깨진다 |
| 5ch 는 HTML 이 아니다 | `norm_ws()` 를 걸면 줄이 다 붙는다 | **HTML 에만** 공백을 뭉갠다 |
| `★2` 무시 | 続きスレ라 실제는 `(순번−1)×1000＋レス` | 안 세면 5,000레스가 `(120)` 으로 보여 밀린다 |
| ヤフコメ 행 경계 | `newsFeed_item` 은 **없다**(`_body` 뿐이고 링크보다 뒤) → 0건 | `<li data-ual-view-type="list">` 로 자른다 |
| Togetter 행 클래스 2종 | ` clearfix` 만 잡으면 25건 중 10건 | `[^"]*clearfix` |
| 검색결과 `<em>` | 검색어가 쪼개져 **겹침이 전부 0.00** | `<em>` 을 먼저 지우고 기사 id 로 묶는다 |

## ★★확산은 '사이트 수'가 아니라 '계열 수'로 센다

5ch 세 판과 마토메 두 곳은 **서로 다른 커뮤가 아니다.** 마토메는 5ch 를 베끼고
세 판은 같은 사람들이 판만 옮긴다. 그대로 세면 5ch 화제 하나가 '5개 커뮤 동시 확산'으로
둔갑해 **小町·ガルちゃん 의 진짜 원본 논쟁을 전부 눌러 버린다.**

일본어는 조사가 한 글자라 글자쌍이 우연히 겹치기도 쉽다 —
겹침 하한을 **7쌍**(한국판 4쌍)으로 올렸다. 안 올리면
`こんなテレビ番組が見たい` 가 `こんなところになぜ食パン投棄` 에 붙는다.

## ★★절대금지 층 — 한국판에 없는 부분이다

일본 커뮤 상위권은 **국적·정치·연예인 두들기기**가 태반이다.
실측 2,517건 중 **392건(15%)** 이 걸렸고 **그것들이 반응은 제일 컸다.**

`혐오·국적`(在日·嫌韓·外国人犯罪) · `정치대립`(정당·首相·**知事·市長**) ·
`실명범죄·불행`(逮捕·不倫·**死亡·事故·重体**) · `성인` · `실황·잡담` · `창작물`.

- **말끝만 다르면 그냥 통과한다.** `死亡事故` 만 막았더니 「4人が死亡」이 1위,
  `死亡` 을 막았더니 「接触し重体」가 2위로 올라왔다. **어간까지 내려 잡아라**
- **시트 맨 아래 '만들지 마라' 칸을 반드시 읽어라**

## ★앙케트 잡담이 반응으로 밀고 올라온다

ガルちゃん·小町 상위의 태반이 앙케트다(`2人目を迷っています`·`海外旅行に行ったことない人`).
코멘트는 크게 붙지만 사실이 하나도 없어 대본을 못 쓴다.
**물음표 없는 쪽이 더 많다** — 일본어는 체언으로 끊는다. 그래서 둘로 건다:
짧은 의문형(−0.40) + **나열형**(짧고·숫자도 인용도 없고 `人`·`こと`·`事` 로 끝남, −0.45).

알맹이 가중을 한국판(0.40)보다 센 **0.55** 로 둔 것도 같은 이유다.

## 이 채널이 만들 수 있으면서 댓글이 터지는 자리

대전제로 정치·국적·연예인을 다 빼면 남는 노른자는 둘이다 —
**`生活・マナー論争`** 과 **`世代・価値観`**. 발언소마치·가루짱의 주력이고
뇌전구의 반전 구조와도 그대로 맞는다. `安全` 열이 `+0.45` 이상인 것들이다.

## 재사용 스크립트 — 전부 `ref_jpcomm/` 에 있다

`hunt.py` (발굴·점수·절대금지·교차확인) · `shapes_jpc.py` (꼴 + 대전제 안전 + 절대금지)
· `jpcomm_spec.md` (실측 기록) · 결과는 `ref_jpcomm/out/`
· 중복목록 `ref_jpcomm/seen.json` (쇼피드 전용)
· 소재 준비(`brief.py`)는 아직 없다 — 지금은 시트의 기사 링크를 직접 쓴다

---

# 커뮤형 소재 발굴 — 커뮤니티 11곳에서 '진짜 터진 것'만 긁어 온다

## 부르는 법

> `커뮤 소재 뽑아 줘` · `오늘 커뮤에 뭐 떴냐` · `<번호>번으로 가`

```
python ref_comm/hunt.py                  # 커뮤 11곳 360여 건 → 점수순 (약 45초)
python ref_comm/hunt.py --no-news        # 뉴스 확인 끄고 빠르게 (2초)
python ref_comm/hunt.py --only 잡학       # 갈래 좁히기
python ref_comm/brief.py --pick 2        # 고른 사건의 기사·사진·시트 준비
```

**매일 05 / 11 / 16 / 21시에 저절로 돈다** (윈도우 작업 스케줄러, 2026-08-21 등록).
실시간 베스트는 **54분에 38%가 갈린다**(루리웹 97%·개드립 100%). 한 번만 돌리면
그 순간 걸린 것만 본다 — `--accum-hours 24`(기본값)가 지난 실행분을 합쳐 준다
(실측: 378건 수집 → 누적 709건). 결과는 폰으로도 날아온다.

**★경제 헌터(06/13/20)와 시각이 겹치면 안 된다.** 둘 다 네이버 검색을 두드려서
동시에 돌면 빈 껍데기가 온다(아래 함정표). 그래서 일부러 1시간씩 벌려 놓았다 —
시각을 옮길 일이 있으면 이 간격을 유지해라.

시작 전에 `ref_comm/comm_spec.md` 를 읽는다. 아래는 요약이다.

## ★커뮤는 소재가 아니라 레이더다

네이버 기사는 **매체가 책임지는 보도**지만 커뮤 글은 **개인이 쓴 미검증 주장**이고
남의 저작물이다. 그대로 옮기면 오보가 되고 베끼는 게 된다.

**커뮤로는 "뭐가 터졌나"만 잡고, 사실관계는 기사에서 가져온다.**
`brief.py` 는 일부러 커뮤 본문을 안 긁는다 — 그 사건을 보도한 네이버 기사를 찾아
검증된 `ref_econ/brief.py` 에 넘긴다. 커뮤 반응은 `comm_context.txt` 에만 남아
**훅("커뮤니티가 뒤집힌 이유")** 으로 쓴다.

- 시트의 `기사` 열이 **`❌0` 이면 만들지 마라**
- 건수가 많아도 **겹침이 0.2 미만이면 딴 사건**이다 (교회 글이 무관한 북카페 기사 7건을 물어 왔다)
- 캡쳐를 쓸 거면 **닉네임·프로필 사진·커뮤 로고를 가린다**

## ★조용히 틀리는 곳 — 다섯

| 함정 | 증상 | 대응 |
|---|---|---|
| **태그 안 줄바꿈** | `<li class="li\n li_best2…">` 라 패턴이 안 잡힌다. **펨코·아카 0건, 디시 49→2건** | 파싱 전 `norm_ws()` 로 공백을 뭉갠다. 덤프 스크립트가 공백을 먼저 뭉개서 **눈으로도 안 보인다** |
| 사이트별 눈금 | 디시 추천 890 · 클리앙 추천 1 — 섞어 세우면 디시가 독식 | 그 사이트 중앙값 대비 배수로 환산 |
| 공지·이벤트 | 반응이 커서 상위 독식 (첫 실행 1·2·3위가 더쿠 공지였다) | `NOTICE` 목록 + 공지행 필터 |
| 검색어 말머리 | `[좆됨] 법인 파산…` → 0건 / 말머리만 떼면 10건 | `news_query()` 가 말머리·확장자·자음·은어를 뗀다 |
| 네이버 몰아치기 | 동시 4건↑ 이면 **빈 껍데기(30KB)** — 진짜 0건과 구별 불가 | 동시 3건, 80KB 미만이면 쉬었다 재시도 |

## 갈래 — 경제·정치는 뺀다 (다른 채널에서 다룬다)

`--drop 정치,경제` 가 기본값이다. 오늘치 기준 이슈 185 · 유머 99 · **잡학 30** · 정치 22 · 경제 10.

**★갈래로 자르지 마라, 알맹이로 잘라라.** 유머라서 못 쓰는 게 아니라
알맹이가 없어서 못 쓰는 것이다 — 잡학에도 0.00 이 수두룩하고
(`싱글벙글 절이나 교회를 다니지 마십시오`), 유머판에도 +0.30 이 있다
(`역대급 경제위기라는 이탈리아 근황`). 경제·정치만 빼고 나머지는 남긴 뒤
`fit` 으로 세우고 **시트의 갈래를 보며 눈으로 고른다.**

**잡학**(`~하는 이유`·`~의 비밀`)이 뇌전구와 결이 제일 맞지만 **물량이 제일 적다.**

## ★소스를 늘려 잡학을 채우려 하지 마라 — 해 봤고 실패했다

디시 원본 갤러리를 직접 긁었더니 **미스터리갤=음모론(UFO·백신) · 역사갤=역사왜곡
· 대체역사갤=팬픽 · 싱갤=잡담/짤** 이었다. 넷 다 안 넣었다.

실베에서 본 `싱글벙글 F1에서 상식적인 게 금지된 이유` 는 싱갤의 결이 아니라
**실베가 그중 좋은 것만 걸러 올려준 결과**다. **디시 실베·힛갤이 이미 필터다 —
그 앞단을 긁지 마라.** 답은 소스가 아니라 **누적**이다(위 `--accum-hours`).

## ★펨코는 `/best` 만 긁는다 — 일반 게시판을 건드리면 IP 가 막힌다

`/mystery` 를 몇 번 두드렸더니 **HTTP 430 `에펨코리아 보안 시스템`** 이 뜨고
**`/best` 까지 같이 막혔다**(2026-08-21 실기). IP 단위라 코드로 못 푼다 — 쉬는 수밖에 없다.

포텐터짐(`/best`)은 **이미 전 게시판을 모아 주고**(`category` 로 어느 판인지 나온다),
행 클래스에 **정치 여부까지 박혀 있다**(`li_best2_politics0/1`). 더 긁을 이유가 없다.

시트의 **`⛔ 차단` 과 `⚠ 0건`(구조 변경)을 구별해서 봐라 — 대응이 정반대다.**

## 뚫리는 곳 / 막힌 곳

**뚫림 11곳** — 더쿠(여) · 펨코(남) · 디시실베(남) · **디시힛갤(유머)** · 네이트판(여)
· 루리웹(남) · 클리앙(시사) · 보배드림(시사) · 아카라이브(남) · 인벤(남) · 개드립(유머).
반응 수치가 **목록 HTML 에 이미 다 있어** 네이버처럼 기사별 API 를 안 불러도 된다(1.7초).

**막힘** — 인스티즈·오늘의유머(Cloudflare) · 뽐뿌(403) · SLR클럽(고사).
뺀 곳 — MLBPARK(반응 수치 없음) · 82쿡(갱신 느림) · 이토랜드(약후) · 웃대(이슈성 낮음).

**가장 센 신호는 교차 커뮤 확산이다.** 성향이 다른 판(더쿠+펨코)에 동시에 떴으면 크게 터진 것이다.

## ★커뮤만 쓰지 마라 — 네이버 섹션을 나란히 돌린다

사장님이 뺀 건 **경제·정치**지 뉴스 전체가 아니다. 알맹이 밀도 실측(2026-08-21):

| 소스 | 하루 | 알맹이 0.25↑ |
|---|--:|--:|
| 네이버 IT/과학 | 79 | **48%** |
| 네이버 사회 | 81 | **47%** |
| 네이버 세계 | 82 | 43% |
| 네이버 생활문화 | 78 | 26% |
| 커뮤 11곳(경제·정치 뺀) | 483 | **15%** |

```
python ref_econ/hunt.py --sid 102,103,105,104    # 사회·생활문화·IT과학·세계
```

**새로 만들 게 없다** — 경제 헌터가 `--sid` 를 받고 반응 실측·사건 묶기까지 그대로 한다.

- ※격차를 곧이곧대로 믿지 마라. 알맹이 자가 `공개·논란·1위·최다` 에 점수를 주는데
  그건 뉴스 제목에 원래 흔하고, 같은 사건이 매체별로 중복된다(독파모 5건·중수청 3건).
  **실제 격차는 3배보다 작다.**
- **커뮤를 버리지는 마라.** 새덕후 논란처럼 **커뮤에서 먼저 터지고 기사가 따라오는 것**이
  있고, 그게 이 채널의 차별점이다.

## 구글 트렌드는 소재원이 아니라 가산점이다

`https://trends.google.co.kr/trending/rss?geo=KR` — 공식 피드라 막힐 일이 없다.
낱말 10개 + 검색량(`ht:approx_traffic` 2000+/500+/100+) + 관련 기사 3건씩.

낱말만 와서 그 자체론 소재가 못 된다. **커뮤 글이 트렌드 낱말과 맞으면
그 커뮤 안에서만 도는 게 아니라는 뜻**이니, 경제 헌터의 '전 섹션 랭킹 진입'과
같은 자리에 쓴다.

## 재사용 스크립트 — 전부 `ref_comm/` 에 있다

`hunt.py` (발굴·점수·뉴스교차확인) · `brief.py` (기사 찾아 경제 브리퍼로 넘김)
· `daily.cmd` (예약 실행 래퍼 — ★ASCII 만 쓴다) · `comm_spec.md` (실측 기록)
· 결과는 `ref_comm/out/` — **`_최신시트.json` 은 안 만든다**(`hunt_*.json` 중 최신을 봐라)
· 중복목록은 경제와 공용 (`ref_econ/seen.json`, `seed_seen.py`)

---

# 일본판 뇌전구

## 부르는 법

> `<링크> 일본판 뇌전구로 줘`

이 말이 나오면 아래를 **전부 적용해** 끝까지 만든다. 되묻지 말고 진행한다.
소재 종류(youtube·news·text)는 링크를 보고 알아서 정한다.

## ★대전제 — 비난 금지. 시청자가 일본인이다

이 채널은 **일본 사람이 보는 일본어 채널**이다. 굳이 비난해서 미움을 살 이유가 없다
(사장님 지시 2026-08-20). 한국어 소재를 그대로 옮기면 자기 나라를 나무라는 영상이 되어
시청자가 등을 돌린다. 다른 규칙보다 이게 먼저다.

**비난은 아예 하지 않는다.** 정부 비판·정책 비판도 마찬가지다 —
"나라가 아니라 정부를 깐 것"은 시청자에게 통하는 변명이 아니다.
사실은 손대지 않는다. **화살표 방향만 바꾼다.**

| 안 되는 것 | 되는 것 |
|---|---|
| 주어가 **일본**이고 서술어가 실책·후퇴·양보 | 주어가 **상대·사건**(ロシアが / プーチンが / 〜が起きた) |
| `日本が自ら減らした4島` (일본이 스스로 줄였다) | `ロシアが踏んだ島` · `4島に減った経緯` |
| `日本の主張には穴がある` | `なぜ4島だけなのか` — 의문형으로 열어 둔다 |
| `日本政府が譲歩しすぎた` | `そういう経緯があった` — 평가어를 뺀다 |
| 결론이 "일본이 잘못했다" | 결론이 "이런 경위가 있었다" · 판단은 시청자에게 |

- **일본 쪽에 불리한 사실도 지우지 않는다.** 지우면 거짓이 된다.
  경위로 담담히 놓고 **평가·질책의 말만** 뺀다
- **한국을 끌어들이지 않는다.** 한일 대비 구도는 어떤 형태로도 넣지 않는다
- **한국 매체·정당 논평을 그대로 옮기지 않는다.** 소재로 쓰더라도 논평은 덜어내고
  사실 경위만 남긴다. 인용이 필요하면 일본 매체·일본 논자의 것을 쓴다
- 오프닝 카드와 마지막 컷이 특히 위험하다 — **둘은 반드시 다시 읽어 본다**

2026-08-19 치시마 회차가 이걸 어겼다. 카드가 `日本が自ら減らした4島` 였고
마지막 컷이 한국 정당 논평(`共産党は全千島を求める`)이었다. **본으로 쓰지 마라.**

## 서버가 한국어에 묶여 있는 곳 — 4군데, 전부 우회해야 한다

뇌전구 프리셋은 한국어 채널이라 아래가 **일본어에서 조용히 망가진다.** 오류가 안 나므로 반드시 직접 챙긴다.

| 곳 | 증상 | 대응 |
|---|---|---|
| 줄나눔 | 공백 기준이라 일본어가 1줄로 뭉쳐 화면 밖으로 잘림 | 한 줄 **전각 12자 이하**로 직접 맞춘다. 13자면 반려된다 |
| `title.card` 검증 | 한글 음절을 세므로 일본어는 무조건 "너무 짧음" 반려 | `script` 단계만 **한국어 카드**로 통과시키고, `subtitle` 단계에서 일본어로 바꿔 넣는다 |
| `grammar.estimated_sec` | 일본어는 0음절이라 **항상 0** — 길이 게이트가 무효 | 서버 값을 믿지 말고 아래 실측 계수로 직접 계산한다 |
| TTS 텍스트 | 숫자를 한글로 정규화 (`閉店1時間前`→`閉店일時間前`) | 서버 job 을 그대로 쓰지 말고 숫자를 되돌린다 |

## 길이 — 실측 0.171초/자

한국어 계수(0.134)를 쓰면 안 된다. 일본어는 한자 1자가 2~3모라라 더 느리다.

| 목표 | 총 글자수(카드 포함) |
|---|---|
| 30초 | 175자 |
| **35초 (상한)** | **204자** |

안전 여유를 두고 **188자 이내**. 컷 12~15개, 밈 컷 3~4개.
※ 한자 비율이 30%를 넘으면 더 느려진다. 첫 회차 실측은 162자 → 27.8초였다.

## 화자 — Typecast

일본 쇼츠에서 실제로 많이 쓰이는 3인으로 고정한다.

| 역할 | 화자 | 비고 |
|---|---|---|
| NARR | Sakura Tanaka | 0.158초/자 |
| CHAR | Kenta Yoshida | 0.124초/자 |
| PUNCH | Haruna Suzuki | 0.146초/자 |

- `language` 는 **`"JPN"`** 이다. `ja`·`ja-JP`·`JAP` 는 422 로 거부된다.
- 화자 ID 는 이름으로 조회한다 (`/v1/voices` 응답에 언어 필드가 없다).
- `Ryu Kojima` 는 이 계정에 없다.

## 글꼴 — 서버가 박는 글꼴로는 한자가 안 나온다

에스코어드림·SB어그로는 **한자 커버리지 0**이다. 그대로 두면 한자만 시스템 글꼴로 폴백한다(오류 없음).
`subtitle` 단계 뒤 `patch_font_jp.py` 로 `sub.ass` 의 Fontname 만 바꾼다. 크기·색·좌표는 건드리지 않는다.

| 원래 | 바꿀 것 |
|---|---|
| SB 어그로 Bold / 에스코어 드림 7 ExtraBold / 여기어때 잘난체 OTF | `Noto Sans JP Black` |
| 에스코어 드림 6 Bold | `Noto Sans JP` |

- RED(밈)는 잘난체 기준 73이라 그대로 두면 작다 — **96으로 올린다.**
- 폴백 검증은 **잉크량 비교로 안 된다**(가나만 그려져 값이 달라짐). 프레임을 뽑아 눈으로 본다.

## 문체 — 번역투를 버린다

한국어 대본을 옮기듯 쓰면 한자가 많아져 딱딱해진다. 일본어 구어는 **한자를 줄이고 가나로 푼다.**

- 보조용언·형식명사는 가나로 — `無い→ない` · `事→こと` · `出来る→できる` · `時→とき`
- 문어체 접미 금지 — `〜し出す` 대신 `〜し始める`/`〜する`
- 조사 확인 — 가능형 자동사는 `に` (`唐揚げには勝てない`, `唐揚げだけは勝てない` 아님)
- 밈 컷에 `勝者`·`開始` 같은 딱딱한 한자어 대신 구어 명사
- 어미는 쇼츠 관용 — `〜なんだよね` · `〜じゃん` · `〜の` · 체언종지. `です・ます` 는 쓰지 않는다
- 상품명은 실제 일본 표기 — 슈퍼 반찬에 `定価` 는 틀리다(책·정찰상품 용어)

## 이미지

- `place` 는 **영문**으로 준다(`"Japan"`). `"일본"` 을 주면 프롬프트에 `in 일본` 이 박혀 간판이 한글로 나올 수 있다.
- 프롬프트에 `Japanese people, Japanese-language signage and kana text` 를 넣는다.
- 인물이 여러 컷에 나오면 `cast` 로 인상착의를 고정한다.

## 재사용 스크립트

`gen_img_jp.py` · `gen_tts_jp.py` · `patch_font_jp.py` · `imgfacts_jp.py` · `render_jp.py` · `final_jp.py` · `verify_jp.py` · `recover_jp.py`

## ★프레임 검증은 '존재'가 아니라 '내용'으로 한다

`img43/` 가 다른 세션에 덮어써져 **이전 회차 사진이 그대로 완성본에 들어간 사고가 있었다**(2026-08-14).
프레임이 구워졌는지만 보면 못 잡는다. `recover_jp.py` 처럼 **원본을 resize·crop 한 결과와 픽셀로 대조**한다.
망가졌으면 `cache/img/<key>.png` 에서 되돌린다 — 프롬프트가 같으면 재생성 비용이 없다.

---

# 인물형 (볼케이노 프리셋 — 대본은 지침서, 렌더는 로컬)

## 부르는 법

> `<인물> 인물형으로 줘`  ·  `<링크> 인물형`

되묻지 말고 끝까지 진행한다. 언어를 안 밝히시면 **한국어**다.
`한국어 일본어 둘 다` 라고 하시면 두 판을 뽑는다 — 컷을 공유해 두 번째 판은 거의 공짜다.

## ★대본 원본은 지침서다 — 시작 전에 반드시 읽는다

```
C:\Users\kjg34\Desktop\hitomi_downloaded_youtube\지침서모음\인물 서사형 지침서.txt
```

아래는 요약일 뿐이고 **어긋나면 지침서가 이긴다.**
(`인물 서사 스토리텔링 지침서 v.6.txt` 는 같은 내용이다)

### ★그 폴더를 먼저 봐라 — 지침서가 12개 있다

정치쇼츠 · 스낵형(3종) · 편집점 · 쇼핑 · 햄숏 · 에센 멀티유즈 · 지침서를 만드는 지침서 …
**볼케이노 폴더 밖(`hitomi_downloaded_youtube\지침서모음\`)이라 눈에 안 띈다.**
새 형식을 요청받으면 **여기부터 뒤진다.** 없다고 단정하고 역설계하지 마라 —
2026-08-19 에 "인물형 템플릿은 없다"고 답했다가 틀렸다.

## 서사 구조 — 5단, 엄격하다

| 단계 | 역할 | 쓰는 법 |
|---|---|---|
| **1 도입** | 훅 · 인물 소개 | **큰따옴표 인용문으로 연다.** 다음 컷에서 `~가 한 말임` 으로 인물을 밝힌다 |
| **2 고난** | 밑바닥 설정 | 빈곤·한계·혹평·무명을 **담담히 나열**한다 |
| **3 계기** | 반전의 시작 | 뜻밖의 조력자, 무식할 정도의 독기, 과감한 결단 |
| **4 성공** | 극적 대비 | **객관적 수치와 타이틀**을 쥐는 순간 (차트·시청률·세계 1위) |
| **5 여운** | 주제 의식 | 초심·숨은 선행·삶을 관통하는 한마디로 묵직하게 닫는다 |

**마지막 컷은 대비 문장이다** — `A였던 사람이 B가 됨` 꼴. 지침서 예시 3편이 전부 이 형태다.

## 문체

- **다큐멘터리식 건조함.** `눈물겨운`·`너무나 슬픈` 같은 감정 수식어를 쓰지 않는다.
  슬플수록 팩트만 나열해 시청자가 스스로 느끼게 한다
- **명사형 종결로 통일** — `~음/~었음`(과거 사실) · `~임/~였음`(상태·규정) · `~함`(진행·강조)
- **한 줄 15~20자**(공백 포함). 줄글 형식의 긴 문장은 절대 쓰지 않는다

## 어휘 — 이걸 안 쓰면 채널 목소리가 안 난다

- **반전 접속사**: `근데`(가장 많이 쓴다) · `그러다` · `결국` · `그래서` · `그제야`
- **사실을 키우는 부사**: `진짜` · `그야말로` · `줄줄이` · `무식하게` · `독하게`
- **관용 표현**: `~로 빵 터짐` · `~가 제대로 사고쳤음` · `~에 처박혀 있었음` ·
  `~로 판 뒤집음` · `듣보` · `꼬리표가 따라다녔음`

## 길이 · 컷

| | |
|---|---|
| 컷 길이 | **2.0초 고정** (지침서 SRT 예시가 전부 0→2→4초다) |
| 컷 수 | **23~27개** |
| 총 길이 | **45~55초** (사장님 지시 2026-08-18). 2초 × 24컷 ≈ 48초가 기본 |
| 나레이션 | **없다.** TTS·화자·전사 키가 전부 무관하다 (`keys_needed: 0`) |
| 소리 | BGM 한 장. `afade` 인아웃 + `loudnorm I=-16` |

## 제목

**인물 이름을 넣지 않고 상태로 부른다** — `백수로 살던 반지하 대학생` ·
`수천억 제안 거절한 고졸 청년` · `빚쟁이에 쫓기던 단칸방 소녀`.
화면 제목띠와 유튜브 제목은 달라도 된다.

## ★서버 `person_render` 를 부르지 마라

토큰이 필요한 건 이 단계 하나뿐이고 **안 거쳐도 그림이 나온다.**
`person_render` 는 렌더러가 아니라 계획기다(응답이 `jobs`·`do` 다). ffmpeg 은 어차피 로컬에서 돈다.
2026-08-13 한화 김승연 회장 회차가 이 길로 완성됐고(`captions.ass` + `cuts.txt` concat),
08-16 오타니·08-17 젠슨이 막힌 건 그때부터 서버 단계를 정식 경로로 썼기 때문이다.
**토큰 찾기로 가지 마라** — 안 뚫리는 게 맞고 애초에 필요가 없다.

## 화면 — 08-13 회차 픽셀 실측

| | |
|---|---|
| 해상도 | 1080×1920 · 30fps |
| 제목 2줄 | `an8` `pos(540,304)` / `pos(540,407)` · 전체 길이 유지 |
| 자막 | `an8` `pos(540,1301)` / `pos(540,1384)` · 컷마다 교체 |
| 박스 | 라운드 사각형 · 높이 = 글자크기 × 1.146 · 모서리 8 · 좌우 여백 24 |
| 색 | 자막 박스 노랑 `#F9F07E` · **제목 박스 흰색** · 글씨 검정 |

**제목에도 박스를 깐다.** 08-13 은 생성 이미지가 단순해 검정 글씨가 그냥 읽혔지만,
실제 보도사진은 밝고 복잡해서 글씨가 묻힌다(실측 2026-08-18 우승 세리머니 컷).

한 줄 15~20자를 화면 폭에 넣으려면 두 줄로 접어야 한다 — 빌더가 폭 초과를 잡아 반려한다.

## ★영상 비중 — 정지 슬라이드쇼로 만들지 마라

젠슨·오타니 회차는 소재가 **전부 생성 이미지**였고 컷 안 움직임이 `0.00` 이었다. 최하 등급이다.
움직임은 컷 안 연속 프레임의 평균 절대차(0~255)로 잰다 —
`0~0.6` 정지 · `1~3` 켄번스 · `6~17` 실제 동영상.

- 사진에는 **켄번스 패닝을 실제로 건다** (1.18배로 잡아 그 안에서 이동)
- **얼굴 없는 스톡 동영상**으로 1/3쯤을 채운다. `ref_person/fetch_stock.py` (Mixkit) 로 받는다
- 비중이 **35% 이상**이면 화면이 산다 (야말 9/24 = 38%)

### ★영상을 빼서 사고를 피하려 하지 마라 — 2026-08-19 에 그렇게 망했다

08-18 에 얼굴 있는 스톡이 주인공으로 읽히는 사고를 지적받자, 그 대응으로
**영상을 통째로 빼고 사진만 24컷**을 썼다. 빌더 `check()` 의 영상비중 게이트까지 지웠다
(`# 사장님 지시로 주인공 사진만 쓰는 회차가 있다` 라는 주석을 달아서).
결과는 정지 슬라이드쇼였고 08-13 한화 회차의 화면과 딴판이 됐다.

**규칙은 '영상을 쓰지 마라'가 아니라 '얼굴 있는 영상을 쓰지 마라'다.**
게이트는 30% 미만이면 반려한다. 사진만으로 갈 회차는 `--photo-only` 로 **명시**해야 한다 —
조용히 0%가 되면 안 된다. 배정은 `assign_vid.py` 를 본떠 쓰고 후보는 `_vsheet*.jpg` 로 눈으로 고른다.

## ★얼굴 있는 스톡은 "그 사람"으로 읽힌다

가장 크게 틀린 자리다(2026-08-18). `주민 절반이 빈곤선 아래임` 아래에 밤거리에서 웃는 여성이,
`여섯 살에 스카우트가 옴` 아래에 성인 남자가 들어갔다. 스톡 라이선스와 무관하게
**실제 사람에게 부당하고, 보는 사람은 그를 주인공으로 읽는다.**

영상 컷은 **얼굴이 드러나지 않는 것만** 쓴다 — 빈 골목·낡은 벽·그래피티·경기장 항공·
네트 너머 잔디·발과 공·조명. 사람이 나오면 뒷모습이거나 아주 작아야 한다.
`sheet_vid.py` 로 후보 시트를 만들어 **눈으로 확인하고** 배정한다.

## 소재 조달

- 인물 사진은 **위키미디어 커먼즈 자유 라이선스**. `ref_person/fetch_photos.py` 를 본뜬다
- 조달기가 **오류 없이** 틀리는 두 곳을 반드시 챙긴다.
  ① 라이선스 문자열은 `CC BY-SA 4.0` 처럼 **띄어쓴다** — `'cc-by'` 하이픈으로 맞추면
     CC BY 계열이 통째로 탈락한다(야말 사진 24장이 전멸했다가 고치니 232/232 통과).
  ② `p00.jpg` 같은 번호 파일명은 **건너뛰지 말고 항상 다시 받는다** — 번호는 검색 순서라
     실행마다 가리키는 사진이 바뀐다. 앞선 실행 파일을 크기만 보고 두면
     야말 자리에 다른 선수가 들어간다(실제로 그렇게 나갔다)
- **CC BY-SA 를 쓰면 동일조건변경허락이 붙는다.** 결과물도 같은 조건으로 공개해야 하므로
  `credits.txt` 를 만들어 설명란에 넣고, 이 조항이 걸리면 사장님께 먼저 여쭙는다
- 실존 인물이 메인이면 **그 인물 이미지는 생성하지 않는다** (볼케이노 공통 규칙)

## 일본어판

인물형은 나레이션이 없어 **뇌전구 일본판 함정 5종이 거의 다 무효다.** 갈아끼울 건 자막과 글꼴뿐이다.

- 글꼴 `Noto Sans JP Black`(제목) / `Noto Sans JP`(자막). 번들 한글 글꼴은 한자 커버리지가 0이다
- 전각이라 한글보다 넓다 — **자막 78 · 제목 92** 로 낮춘다. 89 로 두면 박스가 화면을 넘는다
- 종결은 **体言止め** 로 명사형 종결에 대응시킨다
- 서버 문체 게이트는 로컬로 구우니 **안 걸린다**
- 폴백은 오류가 안 난다 — **프레임을 뽑아 눈으로 본다**

## 하는 일 순서

1. **지침서를 읽는다.** 그다음 인물 취재 → 5단 대본 23~27컷 (컷당 2.0초, 45~55초)
2. 커먼즈 사진 + 얼굴 없는 스톡 영상 조달, 시트로 확인하고 컷마다 배정
3. `python yamal_build.py --lang ko --check` · 일본어판은 `--lang jp`
4. **`python sheet_final.py` 로 완성본 컷 시트를 뽑아 전부 눈으로 본다** — 자막까지 얹힌 실제 화면이라
   소재와 문구가 어긋난 것도 여기서 잡힌다. 이 단계를 건너뛰지 마라
5. `python deliver.py <workdir> "<유튜브 제목>"` 로 납품.
   두 판이면 `v###` 이 같아 `deliver.py` 가 하나만 고르므로 `deliver_both.py` 처럼 파일을 직접 지정한다

## 재사용 스크립트 — 전부 `ref_person/` 에 있다

`yamal_build.py` (빌더 — `--lang ko|jp`, 컷은 언어와 무관해 한 번만 굽는다.
`--check` 로 길이·해상도·움직임·영상비중·**자막 폭 초과**까지 자가검증.
`--photo-only` 는 영상비중 게이트를 끄는 명시 스위치다)
· `fetch_photos.py` (커먼즈 사진) · `fetch_stock.py` (Mixkit 영상)
· `sheet_pho.py` (사진 시트) · `sheet_vid.py` (영상 후보 시트) · `sheet_final.py` (완성본 컷 시트)
· `assign_vid.py` (얼굴 없는 영상을 컷에 배정 — **바뀐 인덱스의 `cut/NNN.mp4` 를 지운다.**
빌더는 길이만 보고 넘겨서 안 지우면 옛 사진 컷이 그대로 남는다)
· `template_out.py` (지침서 §4·§5 출력물 — 불릿 대본 + 📸링크보드 + 💾SRT)
· `deliver_both.py` (두 판 납품 + `credits.txt` — 언어별 최신 `v###` 을 스스로 고른다)
· 납품은 기존 `deliver.py`

**mp4 만 내면 반쪽이다.** 지침서가 정한 결과물은 `template_out.py` 가 뽑는
[제목]+불릿 대본+링크보드+SRT 한 세트다. 렌더 끝나면 이것도 같이 드린다.

회차 예제는 `work_yamal/` (라민 야말, 한국어·일본어 2판). 새 회차는 전용 workdir 를 판다.

**야말 회차 대본은 2026-08-19 에 지침서대로 다시 썼다** — 인용문 도입 · 5단 · 컷 2.0초 고정 ·
`근데`/`그러다`/`결국`/`그제야` · 마지막 대비 문장까지 맞춘 26컷 52초판이니 **본으로 써도 된다.**
(그 전 28컷 51.8초판은 지침서를 모르고 만든 것이었다 — `out/*_v001` 이 그것이다.)
사진이 어느 것인지 눈으로 고르려면 `sheet_pho.py` 로 `img/` 전체 시트를 먼저 뽑는다.

---

# JJACK_J (사장님 본인 채널 — 캡컷 수작업을 자동화한 것)

## 부르는 법

> `<클립 경로> 축구로 줘`  ·  `<클립 경로> 잭제이로 줘`

**기본 납품은 캡컷 드래프트다.** 사장님이 컷·줌을 직접 손보시므로 mp4 로 주지 마라.
mp4 가 필요하면 사장님이 **`mp4로 줘`** 라고 따로 말씀하신다.
**어느 쪽으로 드릴지 되묻지 않는다** — 말씀이 없으면 캡컷이다 (2026-08-19 지시).

되묻지 말고 끝까지 진행한다. 클립만 주시면 프레임을 보고 대본까지 쓴다.
제목이나 대본을 같이 주시면 그걸 쓰고, 안 주시면 내가 쓰고 결과에 무엇을 썼는지 밝힌다.

**볼케이노 MCP 를 쓰지 않는다.** `volcano_video` 를 부르지 마라. 로컬 ffmpeg + ASS 로 끝난다.

시작 전에 `ref_jjack/jjack_spec.md` 를 읽는다. 아래는 요약이다.
(채널 `@JJACK_J` 쇼츠 181편 중 5편 다운로드 → 프레임 픽셀 실측, 2026-08-18)

## 한 줄 요약

**원본 클립 1개 위에 [검정 띠 2줄 제목] + [하단 자막] + [상단효과] 를 얹는다.**
컷 편집이 없다 — 클립은 통으로 흐르고 글자만 바뀐다. 축구·골키퍼 소재.

## 절대 규격 (3편 프레임 실측, 오차 0)

| | |
|---|---|
| 해상도 | 1080×1920 · 30fps |
| 길이 | 실측 12.8~15.5초. **사장님 상한 30초** |
| 검정 띠 | y 0~**521** — 3편 전부 정확히 같다 |
| 영상 밴드 | y 521~1920 (1080×1399), 소스를 꽉 채워 크롭 |
| 제목 1줄 | **흰색**, 잉크중심 y266 |
| 제목 2줄 | **하늘색 `#61E2FA`**, 잉크중심 y406 |
| 하단 자막 | 잉크중심 **y1432**, 흰색 + 검정 외곽선 |
| 강조 | **첫 컷만** 앞부분 주황 `#FF5C24`. 둘째 컷부터는 색 없이 흰색 |
| 상단효과 | 검정 박스 **높이 99px** + 초록 `#00FF00` / 노랑 `#F7FF00` |

**상단효과 색은 빈도가 아니라 역할이다.** 상황을 해설만 하면 **초록**,
대화형으로 주고받으면 **화자1 초록 · 화자2 노랑**. job 에 `"speaker": 1|2` 로 준다.

## 글꼴 — 전부 배달의민족 주아 (`BM JUA`)

- **ScaleX 88%.** 세로로 길쭉하다 — 100% 면 실측보다 8~14% 넓다
- **크기는 줄마다 다르다.** 캡컷에서 폭에 맞춰 키우신 것이라 실측 6줄이 148~227 로 제각각.
  그래서 빌더가 **폭 자동맞춤**으로 푼다. 잉크높이 ≈ 0.754 × fontsize

## 대본 — 첫 줄은 반드시 후킹

`~하고 있다는데?` · `~라는데?` 로 끝낸다. **주황은 후킹의 메인이 되는 앞부분에만.**
**둘째 컷부터는 강조가 없다** — 색 없이 전부 흰색이다 (뒷 컷 `hl` 은 빌더가 반려한다).
하단 자막은 **나레이션 받아쓰기**다 (자동자막 대조로 확인) — 둘을 따로 쓰면 안 된다.

상단효과는 속마음·현 상황 설명이고 `* 문구 *` 꼴이다. **자리를 컷 내용 피해 옮긴다** (x·y 자유).

## ★타입캐스트 준기(화남) — 두 군데서 422 가 난다

`/v1/voices` 가 `emotions: [angry, ...]` 라고 알려주지만 그 이름을 `emotion_type` 에 넣으면 반려다.

| | |
|---|---|
| `emotion_type` | `preset`·`embedding`·`smart` **셋뿐** |
| 감정 이름 | `emotion_preset` 에 따로 → `{"emotion_type":"preset","emotion_preset":"angry"}` |
| `previous_text`·`next_text` | **preset 과 배타적.** 같이 주면 `Extra inputs are not permitted` |

문맥 필드는 `smart` 전용이라 감정 고정을 택하면 포기해야 한다. 동시 3건이면 429 — **2건**으로 돌린다.

## 하는 일 순서

1. 소스 클립 프레임을 **Read 로 직접 본다.** 무슨 상황인지 눈으로 읽어낸다
2. 제목 2줄 + 하단 자막 대본(첫 줄 후킹) + 상단효과를 쓴다
3. `job.json` 쓰고 `python jjack_build.py job.json` — TTS 를 만든다
4. `python jjack_capcut.py job.json --name "이름"` → `python jjack_ccheck.py "이름"`
5. 답변 끝에 **캡컷 프로젝트 경로**와 "캡컷을 껐다 켜야 목록에 뜬다" 를 적는다

`mp4로 줘` 라고 하셨을 때만 3번에 `--check` 를 붙여 굽고 `deliver.py` 로 납품한다.

## ★납품 형태 — mp4 냐 캡컷이냐

**휴먼터치가 필요하면 캡컷 드래프트로 준다.** mp4 는 손볼 수가 없다.

```
python jjack_build.py  <job.json> --check     # TTS + mp4
python jjack_capcut.py <job.json> --name "이름"  # 캡컷 드래프트
python jjack_ccheck.py "이름"                   # 구조 자가검증
```

캡컷 쪽에서 조심할 것 (자세한 것은 `ref_jjack/jjack_spec.md`):

- **맨손으로 세그먼트를 만들면 캡컷이 오류 없이 그냥 안 연다.** 보조 재료
  (`extra_material_refs`) 가 6개씩 매달려 있어서다. 사장님 프로젝트에서 **복제**해 쓴다.
  본으로 쓰는 `친구가 골 막아줬다는데` · `7F561909-...` **두 프로젝트를 지우면 생성기가 죽는다**
- 캔버스는 **720×1280**, 좌표는 정규화 `[-1,1]` 이고 **y 는 위가 +**
- **검정 띠는 크롭이 아니라 1080×1920 PNG 오버레이**다. 캡컷 `Presets` 안에 살아서
  디스크 정리 때 날아간다 — `ref_jjack/assets/band_1080x1920.png` 에 백업해 뒀다
- 자막의 `background_alpha` 가 0.84 여도 `background_style` 이 0 이라 **배경은 안 그려진다**
- **한 텍스트 트랙 안에서 세그먼트가 겹치면 안 된다.** 대화형 상단효과는 트랙을 나눈다
- 사장님은 영상을 **8컷쯤 잘라 일부는 확대·패닝**하신다. 생성기는 한 덩어리로 올려두고
  그 작업은 사람에게 남긴다

## 재사용 스크립트

`jjack_build.py` (mp4 빌더 — 규격이 기본값으로 박혀 있고 `--check` 로 띠·제목·자막·길이·
움직임 자가검증. `--no-tts` 는 캐시만 쓴다) · `jjack_capcut.py` (캡컷 드래프트 생성기)
· `jjack_ccheck.py` (드래프트 구조 검증) · 예제 job 은 `work_jjack_fight/job.json`
· mp4 납품은 기존 `deliver.py`

---

# 짧뷰 (사장님 본인 채널 — 골프. 축구와 형제지만 화면이 다르다)

## 부르는 법

> **`<링크> 골프`**  ·  `<클립 경로> 짧뷰로 줘`

**`골프` 는 이 채널(짧뷰)이다. JJACK_J 가 아니다** — 그쪽은 `축구`·`잭제이` 로 부르신다.
(`work_jjack_golf` 같은 폴더가 있어 헷갈리기 쉬우니 낱말로만 갈라라.)

되묻지 말고 끝까지 진행한다. 소재만 주시면 프레임을 보고 대본까지 쓴다.
**볼케이노 MCP 를 쓰지 않는다.** `volcano_video` 를 부르지 마라. 로컬 ffmpeg + ASS 로 끝난다.

### 소재가 링크로 올 때

사장님은 보통 **링크를 주신다.** 먼저 원본 해상도로 받아 workdir 에 넣고 그걸 소스로 쓴다.

```
python -m yt_dlp --no-update -f "bv*+ba/b" --merge-output-format mp4 -o "<workdir>/src.mp4" "<링크>"
```

받은 뒤 **프레임을 Read 로 직접 봐서** 무슨 상황인지 읽고 대본을 쓴다.
밴드가 정사각이라 세로 소재는 위아래가 크게 잘린다 — 어디를 남길지 프레임 보고 정한다.

## ★납품 형태 — 캡컷 드래프트다. 되묻지 않는다

축구와 같다 — **기본 납품은 캡컷 드래프트**고, mp4 는 사장님이 **`mp4로 줘`** 라고
따로 말씀하실 때만 굽는다 (2026-08-19 지시).

**`nono_capcut.py` 를 2026-08-21 에 만들었다** (골프볼 회차). 새로 짜지 마라.
`jjack_capcut.py` 를 import 해 쓰고 화면 구성만 갈아끼운 것이라 짧다.

- 검정 띠가 없다 → **흰 배경+헤더 PNG 가 영상 '아래'** 로 간다 (축구는 띠가 '위')
  배경 한 장은 `ref_nono/assets/bg_shortview.png` 에 구워 뒀다 — 지우면 생성기가 죽는다
- 영상이 화면을 안 채운다 → 소스를 **1:1 로 미리 크롭**해 `capcut_source` 로 준다.
  캔버스 스케일 0.9, 세로 이동 +0.0427 (밴드 중심 y919 → 정규화)
- 제목은 **노랑 한 줄** — 본에 `#F9EF4D` 가 없어 흰색을 복제해 색만 갈았다
- **`clips` 를 컷 조각으로 올린다** (축구는 통짜). 조각 길이 = 배정된 자막 span + GAP,
  마지막 조각만 + TAIL — `nono_build` 와 같은 셈법이라야 mp4 와 어긋나지 않는다
- **자막 줄나눔은 `sub.ass` 에서 읽어 온다.** 글자수로 다시 접으면 빌더와 갈린다

### ★`font_size` 는 여전히 눈으로 확인받을 값이다

캡컷 단위와 ASS 픽셀의 환산비 `CC_PER_ASS = 6.13` 하나로 푼다. 축구 보정쌍
(자막 `미친 탄력으로…` ↔ 11.0)에서 나온 값이고, **이걸로 계산한 제목 10.4 가
빌더 실측 fontsize 63 과 맞아떨어져** 근거는 선다. 다만 이 채널 본 프로젝트로
직접 잰 값은 아니다 — 캡컷에서 열어 보고 크면 이 상수만 고치면 된다.

시작 전에 `ref_nono/nono_spec.md` 를 읽는다. 아래는 요약이다.
(채널 `@shortview` `UCd8NpX-szthcjWrGmID4BoA` 쇼츠 **174편 전수 메타** + 8편 픽셀 실측, 2026-08-18)

## 한 줄 요약

**흰 배경에 [헤더] · [노랑 제목 한 줄] · [정사각 영상] · [하단 자막] 네 칸을 쌓는다.**
TTS 는 축구와 **완전히 같다**(준기·angry·1.1배). 다른 건 화면뿐이다 —
검정 띠가 없고, 영상이 화면을 안 채우고, 제목이 영상 위가 아니라 **위쪽 흰 칸에** 있다.

## 절대 규격 (8편 픽셀 실측, 편차 ±5px)

| | |
|---|---|
| 해상도 | 1080×1920 · 30fps |
| 길이 | 9~33초. **13~16초가 정답** (85편 평균 27.9만). 빌더 상한 25초 |
| 배경 | **순백 `#FFFFFF`** |
| 헤더 | `(90,138)` 에 410×168 고정 PNG — 8편 전부 같다 |
| 제목 | **G마켓 산스 TTF Medium**, 노랑 `#F9EF4D` + 검정 외곽선 10 + 그림자 3, 잉크중심 **y370** |
| 영상 밴드 | **x54~1025 · y437~1401 = 972×965 (거의 정사각)** |
| 하단 자막 | **주아체**, 흰색 + 검정 외곽선 7, 잉크중심 **y1496**, 2줄이면 줄간격 77 |

**밴드가 정사각이라 세로 소재는 위아래가 크게 잘린다.** 가로·정사각 소재를 골라라.
**길이는 짧을수록 좋다** — 20초를 넘기면 성과가 꺾인다. 13~16초에 붙여라.

## 축구와 다른 점만 추리면

- 제목이 **한 줄**이고 **노랑**이다 (축구는 2줄 흰색/하늘색)
- 하단 자막 첫 컷 **주황 강조가 없다.** 전부 흰색이다
- **상단효과(`* 문구 *`)가 없다**
- 밴드 안에서 **컷이 갈린다** (축구는 통짜). 전환이 자막 교체 시점과 대체로 맞물린다
  → job 에 `clips` 로 자막 컷 번호에 클립을 배정한다. 소스 하나면 `source` 로 통짜

## ★자막 세로 보정 — 빼먹으면 5px 위에 앉는다

ASS 의 `\an5` 는 잉크가 아니라 **글자 상자** 기준이라 주아체는 5px 어긋난다.
`nono_build.py` 의 `calib_sub()` 가 **회색 바탕**에 찍어 재고 밀어준다.
검정 바탕에 찍으면 안 된다 — 검정 외곽선이 배경과 같은 색이라 안 보인다.

검증 창도 **밴드 아래끝(y1401)보다 밑에서 시작**해야 한다. `SUB_CY-120`(y1376)으로
잡으면 어두운 영상의 밴드 밑단이 딸려 들어와 40px 넘게 틀린 값이 나온다 — 한 번 오검출했다.

## 제목·설명란

- **화면 제목과 유튜브 제목은 다르다.** 화면 쪽이 짧고 세다
- **숫자를 넣어라** — 34편 중앙 61,792 vs 없는 140편 33,342 (1.85배)
- 사는 부품 `구력` `전설` `레전드` `클럽` `퍼팅` `골프채` (다만 표본 3~5편이라 배수는 못 믿는다)
  / 죽는 부품 `스윙`(25편 3.2만) `ㅋ`(23편 3.4만) `역대급`(13편 3.4만) — 이쪽은 표본이 단단하다
- 해시태그는 **마지막 한 줄에 몰아쓰고 8개**로 맞춘다 (7개가 최빈이나 8개가 최고). 10개 넘으면 내려감
- **댓글 유도는 살고 구독·좋아요 유도는 죽는다**
- 유튜브 태그(tags)는 안 쓴다. 카테고리 `People & Blogs`

## 하는 일 순서

1. 소스 클립 프레임을 **Read 로 직접 본다.** 무슨 상황인지 눈으로 읽어낸다
2. 노랑 제목 1줄 + 자막 대본 5~9컷(13~16초) 을 쓴다
3. `job.json` 쓰고 `python nono_build.py job.json` — TTS 를 만든다.
   컷을 나누려면 `clips`, 캡컷용으로 **1:1 크롭한 소스를 `capcut_source`** 에 적는다
4. `python nono_capcut.py job.json --name "이름"` → 답변 끝에 **캡컷 프로젝트 경로**와
   "캡컷을 껐다 켜야 목록에 뜬다" 를 적는다
5. `mp4로 줘` 라고 하셨을 때만 3번에 `--check` 를 붙여 굽고
   `python deliver.py <workdir> "<유튜브 제목>"` 로 납품한다

## 조심할 것

- **`ref_nono/assets/header_shortview.png` 를 지우면 빌더가 죽는다.** 원본 프레임에서
  뽑은 것이라 다시 만들려면 `ref_nono/assets.py` 를 돌려야 한다
- 비골프 2편(`F6ZYZyY1UMw` `YMNEYDa9y4U`)은 제목·자막이 **검정 민무늬**다.
  초기 스타일이니 **베끼지 마라** — 노랑 제목 계열이 현재 규격이다

## 재사용 스크립트

`nono_build.py` (빌더 — 규격이 기본값으로 박혀 있고 `--check` 로 배경·밴드·헤더·제목·자막·
움직임 자가검증. `--no-tts` 는 캐시만 쓴다)
· **`nono_capcut.py` (캡컷 드래프트 생성기 — 기본 납품. 검증은 축구 것 `jjack_ccheck.py`)**
· 자산 `ref_nono/assets/bg_shortview.png`(흰 배경+헤더) · 조사 스크립트는 `ref_nono/`
· 예제 job 은 `work_nono_test/job.json`, 컷·캡컷 예제는 `work_nono_balltest_0821/job.json`
· 납품은 기존 `deliver.py`

★**`--check` 의 '밴드' 항목은 밝은 화면에서 거짓 미달이 뜬다.** 흰 실험복·흰 배경이
소재에 있으면 밴드 위끝을 흰 바탕과 못 가려 436 대신 474 로 잡는다. 빌더 필터는
`scale=972:965` → `overlay=54:437` 로 **좌표가 박혀 있어 어긋날 수가 없다** —
의심되면 `filter.txt` 를 보고 넘어가라 (2026-08-21 실측).

---

# 칩칩 (바이럴 클립 재포장 채널 — 화면은 축구와 같다)

## 부르는 법

> `<클립 경로> 칩칩으로 줘`  ·  `<링크> 칩칩이 스타일로`

채널 `UCMkYFJ7J5ELuhQ-lWKGMlXA` · 구독 2,810 · 쇼츠 119편.
**볼케이노 MCP 를 쓰지 않는다.** `volcano_video` 를 부르지 마라.

시작 전에 `ref_chipchip/chipchip_spec.md` 를 읽는다. 아래는 요약이다.
(쇼츠 119편 메타 + 대표 5편 프레임 픽셀 실측, 2026-08-19)

## ★빌더를 새로 짜지 마라 — 화면이 JJACK_J(축구)와 같은 템플릿이다

CLAUDE.md 에 항목이 없어 규격이 없는 줄 알고 새로 만들기 쉽다. **2026-08-19 에 그럴 뻔했다.**
사장님이 두 채널에 같은 캡컷 본을 쓰신다. `jjack_build.py` · `jjack_capcut.py` 가 그대로 굽는다.

| 항목 | jjack_spec | 칩칩 실측 |
|---|---|---|
| 검정 띠 | y 0~521 | y 0~**522** (3편 정확히) |
| 영상 밴드 | 1080×1399 | 1080×**1398** |
| 제목 1줄 흰색 잉크중심 | y266 | y262·265·270 |
| 제목 2줄 하늘색 | `#61E2FA` | `#5FE3FD`대 |
| 상단효과 초록 | `#00FF00` | `#1CFB1D`대 |
| 하단 자막 잉크중심 | y1432 | y1428 |

## 축구와 다른 점 — 제목(검정 띠)뿐이다. 자막은 축구와 같다

**★이 둘을 헷갈리지 마라.** 2026-08-19 에 제목의 체언종지를 보고 자막까지 그럴 거라
넘겨짚었다가 프레임을 보고 뒤집혔다.

### 제목 (검정 띠 2줄) — 축구와 다르다

축구는 `~하고 있다는데?` 지만 칩칩 제목은 **두 줄 다 체언종지(명사 끝)** 다.
**1줄이 수식절, 2줄이 핵심 명사구**로 갈린다.

```
1줄(흰색)    피지컬을 잊게 만드는     ← ~는/~한 으로 끝나는 수식절
2줄(하늘색)  압도적인 춤선           ← 핵심 명사구
```

- 한 줄 6~10자, 두 줄 합쳐 15~20자
- 어휘: `독보적` `압도적` `확신의` `역대급` `절대` `기죽이는` `장악하는` `잊게 만드는`
- 원본이 크게 터졌으면 앞에 **`(2000만뷰)`** 처럼 원본 조회수를 박는다

### 하단 자막 — 축구와 똑같다

첫 컷은 **`~다는데?` · `~라는데?` 후킹 + 주황 강조**다. 프레임 실측 4편 중 3편이 그렇다.

```
뒤 댄서들 [기를 다 죽여놨]다는데?     ← 주황이 가운데
[유치원이] 난리가 났다는데?           ← 주황이 앞
[확신의 센터 상]이라는데?
```

`jjack_build.py` 는 `hl` 이 문장 앞이든 중간이든 다 처리한다. 둘째 컷부터는 색 없이 흰색이다.
주황을 안 쓰는 회차도 있다(`VVmz8_77X4A` 는 전부 흰색) — 빌더는 첫 컷 `hl` 이 없으면
**경고만 하고 통과**시키니 그대로 두면 된다.

- 길이는 **11.7~20.9초** (축구 12.8~15.5초보다 조금 길다). 빌더 상한 30초에 안 걸린다
- 한국어판과 영어/일본어판을 따로 올린다

## ★`제로뷰` 는 별도 계열이다

`kn6ek5xWZZA` (CG를 이긴 기적) 는 **검정 띠가 아예 없다.** 흰 배경 + 파란 헤더 `제로뷰` +
가운데 카드형이다. 위 규격을 여기 적용하지 마라.

## ★소재 조달 — 이 채널의 진짜 병목이다

칩칩 소재는 전부 **타 채널 원본**이다. 재포장은 세 가지가 동시에 걸린다 —
유튜브 **재사용 콘텐츠 정책**(수익화), **Content ID**, 그리고 다수가 **미성년 등장**이다.
허락 없는 클립으로 완성본을 만들지 마라. 만들어 드려도 채널 쪽이 위험해진다.

- **학원·단체 원본(발레학원·유치원·댄스학원)은 허락이 잘 난다** — 홍보가 목적이라 서로 이득이고,
  한 번 받으면 그 채널이 통째로 열린다. 요청 메일 본은 `ref_chipchip/permission_email.md`
- **개인 크리에이터 채널은 어렵다** — 본인 채널 수익이 사업이라 승낙 유인이 없다
- 소재 후보는 `sheet_hunt.py` 로 썸네일 시트를 뽑아 눈으로 고른다

## 성과 — 냉정하게 볼 것

조회 중앙 22,000 / 최고 190만(요행, 중앙값의 86배) · **좋아요율 중앙 0.58%**.
같은 자로 잰 원본 채널들이 4~5% 다 — **8분의 1**이다. 조회는 나와도 팬이 안 쌓인다는 뜻이니,
조회수만 보고 성공을 판단하지 마라.

## 재사용 스크립트

빌더는 축구 것을 그대로 — `jjack_build.py` · `jjack_capcut.py` · `jjack_ccheck.py`.
조사 스크립트는 `work_chipchip_simpson/` 에 있고 다른 채널에도 그대로 돌아간다:
`cc.py` (RSS+카탈로그) · `cc_measure.py` · `cc_measure2.py` (프레임 실측) · `cc_row.py`
· `profile.py` (채널 전수 프로필) · `sheet_ref.py` · `sheet_hunt.py` (후보 시트)

---

# 매일일보형 (시사 클립 재포장 — 화면 규격만 실측해 둔 것)

## 부르는 법

> `<클립 경로> 매일일보형으로 줘`

**볼케이노 MCP 를 쓰지 않는다.** 로컬 ffmpeg + ASS 로 끝난다.
시작 전에 `ref_maeil/maeil_spec.md` 를 읽는다.
(채널 `UCRLwWtZEGyjZqkhCgSWe7_w` 쇼츠 69편 전수 메타 + 5편 픽셀 실측, 2026-08-21.
 원자료 `work_ch_maeil/`)

## 한 줄 요약

소스 클립을 화면 가득 깔고 위를 검정으로 덮어 **[배지] [흰 줄] [시안 제목]** 을 쌓는다.
축구형(JJACK)과 뼈대가 같지만 **검정띠가 고정이 아니고, 오프닝 카드라는 둘째 상태가 있다.**

- 오프닝 카드(0~4초쯤) — 검정 아래끝 **830** · 빨강 배지 · 흰 줄 507 · 시안 대문짝 696 · 금색 꼬리 894
- 본문 — 검정 아래끝 **568** · 흰 제목 300 · 시안 제목 398 · 자막 565(2줄째 675)
- 시안 `#00F4EC` 가 이 채널 서명색. **노랑이 둘이다** — 카드는 금색 `#F8D400`, 자막은 형광 `#ECFC00`
- 글꼴 **여기어때 잘난체 + ScaleX 99%** (실측 잉크상자 대조로 후보 6종 중 폭오차 +11 로 1등)

## ★조심할 것 — 넷

| 함정 | 증상 | 대응 |
|---|---|---|
| 검정띠를 한 장으로 잼 | 띠 아래가 어두우면 **20~50px 부풀어** 잡힌다 | 여러 프레임의 **최솟값**을 취한다 |
| 본문 띠를 고정값으로 봄 | 편마다 **498~619** 로 갈린다 (축구형 521 과 다르다) | 중앙 568 로 박고 허용폭을 넓게 |
| 카드 흰 줄 잉크중심 | **배지의 흰 글씨가 섞여** 431 로 잘못 읽힌다 (참값 **507**) | 색 덩어리를 행으로 갈라서 잰다 |
| 자막 2줄 가운데 정렬 | 첫 줄이 검정띠 **안으로** 밀려 들어간다 | 첫 줄을 565 에 고정하고 아래로 쌓는다 |

## 소리·컷 — 통짜가 아니다

| | |
|---|---|
| 컷 전환 | 편당 **1~6개** (35초에 3개, 55초에 6개) |
| 오디오 | **무음 구간 0건** · mean **-15dB** / max 0dB 로 눌러 놓았다 |
| 업로드 | **2160×3840** 69편 전부 |

빌더가 `clips` 로 `{src,in,out}` 을 잘라 이어붙이고, `loudnorm=I=-15` 로 맞추고,
**4K 로 굽는다**(`--hd` 로 1080). ASS PlayRes 는 1080 로 두고 4K 프레임에 태워
libass 가 2배로 그리게 한다 — 1080 으로 굽고 늘리면 글자가 뭉개진다.

★**나레이션 유무는 확인 못 했다.** 무음이 없어 오디오만으로는 못 가른다 —
  귀로 들어봐야 안다. 얹는 구조면 축구형 타입캐스트(준기)를 그대로 쓴다.

## 자막은 전사로 뽑는다

```
python ref_maeil/asr_subs.py clip.mp4 --into job.json
```

faster-whisper(CPU, torch 불필요)로 발화를 받아쓴다. **자막은 받아쓰기지 창작이 아니다.**

- **줄바꿈을 글자수로 하지 마라.** 빌더의 `ink_box` 를 그대로 불러 **폭으로** 접는다
- 한 장 최대 2줄 · 노출 0.9~3.2초 · 낱말 간격 0.55초면 문장이 바뀐 것으로 본다
- ★**`--offset` 에 `card_secs` 를 주지 마라.** 카드는 소스 앞부분을 *덮는* 것이라
  그 아래로 음성이 그대로 흐른다 — 밀면 자막이 말과 어긋난다.
  카드에 걸린 자막을 빌더가 안 그리는 게 정상이다.
  `--offset` 은 **앞에 다른 클립을 덧붙였을 때만** 쓴다
- ★`asr_subs.py` 에서 `sys.stdout` 을 또 감싸지 마라 — `maeil_build` 가 import 때
  이미 감싸서, 위에 덧씌우면 앞의 것이 detach 되어 죽는다

## ★★브랜드 자산은 베끼지 않는다

- 워터마크 **`매일일보`** 는 **실존 신문사 이름**이다. 그대로 박으면 그 언론사 명의가 된다.
  빌더는 `brand` 를 받아 **사장님 채널명**을 찍는다 — 비우면 경고가 뜬다
- **`긴급뉴스` 빨강 배지**도 그 채널 자산이라 복제하지 않는다. 같은 자리·규격에 **문구를 받아 새로 그린다**
- 좌표·색·글꼴은 규격이라 베껴도 된다. 위 둘만 갈아끼우면 된다

## ★길이가 성과를 가른다

| 길이 | 편수 | 중앙 조회 |
|---|--:|--:|
| ~20초 | 4 | **779,089** |
| 30~45초 | 28 | 279,134 |
| **60초+** | 14 | **20,617** |

**60초를 넘기면 13배로 폭락한다.** 원본 채널은 69편 중 14편을 60초 넘게 올렸다 —
**거긴 따라 하지 마라.** 목표는 25~40초, 욕심내면 20초 아래. 빌더가 60초 넘으면 경고한다.

## 재사용 스크립트

`ref_maeil/maeil_build.py` (빌더 — `clips` 이어붙이기 · 4K 기본 · `bgm` · loudnorm ·
`--check` 자가검증. `--out` 은 **절대경로**로 줘라, ffmpeg 이 job 폴더에서 돈다)
· `ref_maeil/asr_subs.py` (전사 → `subs`)
· `ref_maeil/maeil_spec.md` (실측 기록)
· 예제 job 은 `work_maeil_test/job.json` · 컷 예제는 `job_cuts.json`
· 조사 스크립트는 `work_ch_maeil/` (`measure.py` · `timeline.py` · `clean.py` · `slots.py`
· `truband.py` · `fontmatch.py`) · 납품은 기존 `deliver.py`

---

# 텔레그램 (`ref_notify/`) — 사장님 폰과 주고받는다

**양방향이다** (2026-08-21 2단계). 봇은 `@Jjack2_bot`.

**리모컨이 아니라 우편함이다.** 폰에서 영상 제작을 시작시킬 수는 없다 — 그건 여기 앉아야
한다. 대신 **자리에 없을 때 양쪽으로 쌓아 둘 수 있다**: 헌터 결과·완성본이 폰으로 가고,
폰에서 던진 링크·메모·지시가 창고에 쌓여 앉으실 때 꺼내진다.

| 언제 | 무엇이 | 어디서 부르나 |
|---|---|---|
| **06/13/20시** 경제 헌터 직후 | 상위 5개 + 채널배수 ⭐ | `ref_econ/daily.cmd` 끝줄 |
| **05/11/16/21시** 커뮤 헌터 직후 | 상위 5개 + 교차커뮤 + **기사 유무 ⛔** | `ref_comm/daily.cmd` 끝줄 |
| **06:05/13:05/20:05** 일본 헌터 직후 | 상위 5개 + **대전제 안전도 ⚠️** | `ref_jpecon/daily.cmd` 끝줄 |
| 완성 mp4 가 납품될 때 | 영상 자체 + 파일명·크기 | `deliver_sweep.py` 의 `notify()` |

**세 헌터는 시트 모양이 제각각이다** — 커뮤만 최상위 키가 `rows` 이고 `_최신시트.json` 을
안 만든다(`hunt_*.json` 중 최신을 쓴다). 주소·경과시간 키도 다 다르다.
`notify_hunt.py` 가 소스별로 따로 찍는 이유이니 **하나로 뭉뚱그리지 마라.**

- **토큰은 레포 밖 `~/.volcano/keys/telegram`** 에 있다 (EvoLink·Typecast 와 같은 자리).
  `tg_config.json` 에는 chat_id 만 남았다 — 작업 폴더 안이라 파일을 찍어 보다가
  토큰이 화면·대화기록에 새기 쉬워서 옮겼다. **코드에 박지 말고 어디에도 붙여넣지 마라**
  (살았는지 볼 때는 `tg_check.py`, 새로 넣을 때는 `tg_paste.py` — 둘 다 값을 안 찍는다)
- `tg.py` 는 **절대 예외를 밖으로 던지지 않는다.** 알림이 죽어도 헌터·납품·훅은 살아야 한다.
  실패는 `False` 와 stderr 한 줄로만 알린다 — 이 성질을 깨지 마라
- 봇을 새로 만들거나 대화창을 지웠으면 `python ref_notify/tg_setup.py` 로 chat_id 를 다시 잡는다
- 형식만 눈으로 볼 때는 `python ref_notify/notify_hunt.py --dry`
- 상한: 사진 10MB · 영상 50MB · 글 4096자. 넘으면 `tg.py` 가 걸러 내고 글로만 알린다
  (완성본은 보통 4MB 라 여유가 있다)
- 소스를 늘리려면 `notify_hunt.py` 의 `SRC` 에 한 줄 더하고 그 헌터의 `daily.cmd` 끝에 매단다

## 받는 쪽 — `tg_bot.py`. 정해진 메뉴만 돈다

예약 **`볼케이노 텔레그램 봇`** 이 1분마다 부르고 한 번에 45초를 기다린다.
체감은 상주와 같은데 재부팅에 살아남고, 죽어도 다음 분에 되살아난다.
**`pythonw` 로 돌려 창이 안 뜬다** — 그래서 로그는 화면이 아니라 `ref_notify/bot_log.txt` 다.

| 폰에서 치면 | 하는 일 |
|---|---|
| `시트` · `시트 커뮤` | 최신 시트 상위 5개 (경제·커뮤·일본·일커·축구·골프) |
| `3번` · `3` | 방금 본 시트의 그 번호를 **찜한다** (만들지는 않는다) |
| `찜` | 찜해 둔 목록 |
| `창고` | 폰에서 던져 둔 클립·메모 |
| `상태` | 헌터가 언제 돌았나 · 창고·찜·완성본 개수 |
| `납품` / `납품 1` | 최근 완성본 목록 / 그 영상을 폰으로 |
| **그 밖의 말·링크·사진** | 명령이 아니라 **소재**로 보고 창고에 넣는다 |

★**원격 셸이 아니다.** 이 성질을 깨지 마라 — chat_id 하나만 받고(남이 봇을 찾아 말을 걸어도
무시), 자유 명령·셸·파일경로를 받지 않으며, `VERBS` 에 적힌 낱말만 돈다.

## ★★우편함을 비우는 곳은 하나뿐이어야 한다

텔레그램 `offset` 은 봇마다 하나뿐이다. 수거자가 둘이면 **메시지가 둘로 갈린다.**

`ref_clip/inbox.py` 가 매시간 같은 일을 하고 있었고(예약 `볼케이노 소재창고 매시간`),
그쪽은 **링크 없는 메시지를 버리면서 offset 만 올렸다** — 사장님이 보내신
`5시에 업로드할 …` 지시가 그렇게 사라질 뻔했다(2026-08-21 실기로 잡았다).

- 그 예약은 **껐고**, `ref_clip/inbox.py` 의 수거는 은퇴시켰다(`--list` 만 남았다). 되살리지 마라
- 링크는 여전히 `ref_clip/queue.json` 에 들어간다(앞머리 `축`·`골`·`춤` 으로 채널 배정).
  링크 없는 글·사진은 `ref_notify/inbox/inbox.json` 에 따로 담긴다
- 봇이 도는 동안 `tg_inbox.py` 로는 아무것도 안 보인다(offset 이 밀려서다) — `창고` 로 봐라

## 재사용 스크립트

`tg.py` (보내기 — `send_text`·`send_photo`·`send_video`·`send_doc`, 단독 실행하면 시험 발송)
· **`tg_bot.py` (받기 — `--once` 즉시처리 · `--dry` 화면에만 · `--menu` 메뉴확인)**
· `notify_hunt.py` (시트 요약 — `--src`·`--top`·`--dry`) · `tg_setup.py` (chat_id 잡기)
· `tg_check.py` (토큰이 살았나) · `tg_paste.py` (새 토큰 넣기 — 작은 창) · `translate.py`
· `tg_inbox.py` (날것 우편함 — 봇이 도는 지금은 늘 비어 보인다)
· 상태는 `bot_state.json`(offset·찜) · 창고는 `inbox/inbox.json` + `ref_clip/queue.json`
· 로그는 `bot_log.txt` (1MB 넘으면 스스로 지운다)

---

# 칩칩·ランカー 소재 발굴 — 소스 채널 풀에서 낚는다

## 부르는 법

> **`칩칩`** · `칩칩 소재 뽑아 줘` · `랑카 뭐 만들까` · `<번호>번으로 가`

**`칩칩` 이라고만 하시면 소재를 찾아 달라는 뜻이다** (사장님 지시 2026-08-21).
클립 경로를 같이 주시면 그때가 만들라는 뜻이다.

### ★허락 문제를 꺼내지 마라 — 사장님이 알아서 하신다

지시 2026-08-21. 재사용 정책·Content ID·미성년 등장은 이미 아시는 내용이고
**본인이 처리하신다.** 후보를 내면서 그 이야기를 덧붙이지 마라 — 찾는 게 내 일이다.
`◎/○` 표시는 참고용으로 남겨 두되 설명하지 않는다.

### ★한 소재로 한국·일본 둘 다 만든다

칩칩(한국)과 ランカー(일본)에 같은 클립이 나간다. **어느 쪽 소재냐고 묻지 마라.**
후보를 낼 때 '한국이 셀 것 / 일본이 셀 것' 만 한 줄로 밝히면 된다.

### ★★소재는 **한국·일본을 뺀 나라** 것만 쓴다

지시 2026-08-21. 두 채널이 한국·일본에 나가므로 **그 나라 원본은 시청자가 이미 본 것**이다.
2026-08-21 첫판을 한국어 질의로 짰다가 통째로 헛돌았다 — **질의는 영어가 기본이다.**

- `hunt.py` 의 `QUERIES` 는 영어이고, 검색 URL 에 `hl=en&gl=US` 를 박는다.
  안 박으면 한국 IP 라 국내 클립이 위로 올라온다
- 제목·채널명에 **한글이나 가나가 보이면 뺀다**(`is_domestic`). 한자만 있는 것은
  중국·대만일 수 있어 안 뺀다
- 국내 질의(`QUERIES_KO`)는 남겨 뒀지만 **`--ko` 로만 돈다.** 기본이 아니다

```
python ref_chipchip/hunt.py --scout    # 소스 채널 풀을 넓힌다 (가끔·2분)
python ref_chipchip/hunt.py           # 풀을 재서 오늘의 후보 (매일·3분)
python ref_chipchip/hunt.py --sheet   # 후보 썸네일 시트 → out/_후보시트.jpg
python ref_chipchip/hunt.py --took <videoId> ...   # 쓴 것을 중복목록에
```

시작 전에 `ref_chipchip/hunt_spec.md` 를 읽는다. 아래는 요약이다.
(두 채널 카탈로그 전수 + 같은 원본 클립 71쌍 대조, 2026-08-21. 원자료 `work_hunt_0821/`)

## ★`@ランカー-p7k` 는 칩칩의 일본어 쌍둥이다 — 남의 채널이 아니다

`UCkMJkr6Vc5uiS_pTu_eG12g` · 구독 **11,900** · 쇼츠 113편 · 누적 4,052만.
**칩칩(2,810)보다 4배 크고 천장이 10배 높다.** 같은 클립을 양쪽에 올린 71쌍에서
한국/일본 조회비 **중앙 0.40** — 같은 소재라도 일본이 2.5배 나온다.

`work_chipchip/cand.tsv` 는 '일본엔 올렸고 한국엔 안 올린' 재고 목록이다.

## ★춤 잘 추는 클립은 안 터진다 — 춤 옆에서 벌어지는 사건이 터진다

중앙값은 갈래를 못 가른다(셋 다 3만). **갈리는 것은 천장이다.**

| 갈래 | 편수 | 일본 최고 | 10만↑ 비율 | 점수 배수 |
|---|--:|--:|--:|--:|
| 사건·반전 | 44 | **600만** | **31.8%** | ×1.25~1.35 |
| 실력 자랑 | 19 | 35만 | 26.3% | **×0.85** |
| 탈인형·동물 | 8 | **2,100만** | 25.0% | ×1.15 |

대박 4편(오징어 2,100만 · 결혼식 난입 600만 · 공부방 누나 430만 · 손가락 조종 150만)은
**넷 다 주인공이 춤을 잘 춰서가 아니라 화면에 사건이 있어서** 터졌다.
탈인형 25% 는 **오징어 한 캐릭터가 만든 값**이니 갈래의 힘으로 읽지 마라.

접었던 **TOP5 모음 계열은 되살리지 마라** — 중앙은 비슷한데 천장이 33배 낮다.

## ★★쇼츠 발굴의 유일한 문 — 검색 페이지 원본 HTML

**`yt-dlp` 로는 쇼츠가 안 나온다**(기존 기록 그대로 롱폼만). 그런데 검색 결과 페이지를
curl 로 받아 `ytInitialData` 를 파싱하면 **조회수까지 붙어서** 나온다.

```
https://www.youtube.com/results?search_query=<질의>&sp=EgIYAQ%253D%253D
```

- `sp=EgIYAQ==`(4분 미만)만 `shortsLockupViewModel` 을 준다. 한 질의에 25~40건
- `sp=EgQIAhgB`(이번주)·`sp=CAMSAhgB`(조회순)은 **쇼츠 0건**이다. 날짜·정렬은 같이 못 쓴다
- 쇼츠 카드엔 채널이 없다 → `oembed` 로 캔다. 채널 `/shorts` 도 같은 꼴이라 파서 하나로 된다

## ★재포장 채널은 소스가 아니라 레이더다

풀에 `지구촌픽`·`인포샷`·`짤툰` 같은 채널이 딸려 온다. **칩칩과 같은 일을 하는 경쟁자**라
쓰면 재포장의 재포장이 된다. 기본으로 후보에서 빼고 레이더로만 쓴다(`--with-repack` 로 켬).

## 조용히 틀리는 곳 — 넷

| 함정 | 증상 | 대응 |
|---|---|---|
| 좁은 질의 | `피로연 부녀 댄스` 0건 / `피로연 댄스` 40건 | 낱말을 덜어내며 넓히는 폴백 |
| 두루뭉술한 낱말 | 춤맥락에 `축제` 를 넣으니 **세종대 축제 '소개팅'** 이 1위 | 좁은 낱말만 (`shapes.py` 와 같은 병) |
| 넓힌 질의의 오염 | `아빠` 로 넓히니 스포츠·사연 채널이 상위 독식 | **클립 제목의 춤맥락**으로 채널 자격을 건다 |
| 작은 채널 독식 | 중앙 573인 채널이 배수로 도배 | 바닥 2,000 · 배수 상한 25 |

**시트를 반드시 눈으로 봐라.** 첫 시트 상위에 `멜로디와사람들` 4편이 올랐는데
**1976년 잭슨파이브 방송 아카이브**였다 — 조회수·배수·갈래 어느 값으로도 안 걸린다.

## ★상황판 — 한 장으로 다 본다

```
board.cmd            (볼케이노 폴더에서 더블클릭 — 새로 받아 브라우저로 연다)
python ref_chipchip/board.py --full --open
python ref_chipchip/board.py                 # 내 채널·트렌드만 (빠름)
```

`ref_chipchip/out/board.html` 에 여섯 칸이 한 화면에 나온다 —
**오늘 쓸 소재 · 급상승 · 달아오른 채널 · 내 채널 둘 · 유사경쟁(레이더) · 실시간 트렌드**.
썸네일을 누르면 유튜브로 바로 열린다. ref_ 폴더를 뒤질 일이 없다.

## ★★유튜브 RSS 를 쓰지 마라 — 몰아치면 IP 단위로 404 가 된다

`feeds/videos.xml?channel_id=` 을 **채널 111곳에 연달아** 때렸더니 그 엔드포인트가
막혔다(2026-08-21). **내 채널까지 404** 였고 같은 IP 에서 `/shorts`·검색·구글트렌드는
전부 200 이었다. 하루 이틀 쉬면 풀리지만 코드로는 못 푼다.

- **`/shorts` 페이지 하나로 대신한다** — 최신순 + 조회수를 같이 주므로 게시일 대신
  '앞에서 몇 번째'를 신선도로 쓴다. 요청도 채널당 2회에서 1회로 준다
- **오류 페이지를 크기로 판정하지 마라.** 유튜브 404 는 1,613바이트 HTML 이라
  `size > 800` 을 통과했고, 파서가 조용히 0건을 내며 캐시 179개 중 95개를 오염시켰다.
  `fetch()` 가 이제 내용을 보고 걸러 지운다

## 재사용 스크립트 — 전부 `ref_chipchip/` 에 있다

`board.py` (상황판) · `hunt.py` (발굴·사냥·점수) · `sheet.py` (후보 시트) · `scout_channels.py` (허락 후보 재기)
· `add_credit.py` (원본 크레딧 되살리기) · `permission_email.md` (허락 요청 본)
· `hunt_spec.md` (실측 기록) · 결과는 `ref_chipchip/out/` · 풀은 `pool.json` · 중복은 `seen.json`

---

# 축구·골프 소재 발굴 (`ref_sport/`) — 칩칩과 같은 엔진, 주제만 다르다

## 부르는 법

> **`축구 소재`** · `짹짹 뭐 만들까` · **`골프 소재`** · `짧뷰 뭐 만들까`

되묻지 말고 후보를 뽑아 드린다. 클립 경로를 같이 주시면 그때가 **만들라는 뜻**이다.
칩칩 규칙과 같다 — **허락 문제를 꺼내지 마라.** 사장님이 알아서 하신다.

```
python ref_sport/hunt.py --topic 축구 --scout    # 풀 넓히기 (주1회·3분)
python ref_sport/hunt.py --topic 축구            # 오늘의 후보 (매일)
python ref_sport/hunt.py --topic 축구 --best     # 이미 크게 터진 것 중에서
python ref_sport/hunt.py --topic 축구 --sheet    # 썸네일 시트 (눈으로 고른다)
python ref_sport/hunt.py --topic 축구 --took <videoId> ...
```

`--topic` 은 `soccer`·`golf` 영문 별칭도 받는다. **예약 `.cmd` 는 영문으로 부른다** —
`.cmd` 에 한글 인자가 들어가면 콘솔 코드페이지에 따라 조용히 깨진다 (두 번 데였다).

시작 전에 `ref_sport/hunt_spec.md` 를 읽는다. 아래는 요약이다.

## ★★FIFA 는 무조건 뺀다 (사장님 지시 2026-08-21)

**대회와 게임 둘 다**다. 한쪽만 막으면 반이 샌다.
`fifa` · `world cup` · `ea fc` · `ea sports` · `fc 24~26` · `ultimate team` ·
`pro clubs` · `career mode` · `efootball` · `pes` · `konami`.
`world cup` 을 넣으면 진짜 월드컵 클립도 빠지는데 **그게 곧 FIFA 대회라 맞다.**

**★걸러지는 것은 제목뿐이다.** 모음 영상 안에 게임 장면이 한 컷 섞여 있어도 제목에
안 적히면 통과한다 (첫 시트 19번 화면 목록에 `Fifa Glitch` 가 있었다).
**시트를 눈으로 봐라** — 글자 필터로는 못 잡는다.

## 칩칩과 다른 점 넷

| | 축구 | 골프 |
|---|---|---|
| 검색 로케일 | **`gl=GB`** | `gl=US` |
| 한·일 원본 | 둘 다 뺀다 (神ショーツ에도 나가므로) | **한국만** |
| 재포장 채널 | **후보로 쓴다** | 레이더로만 |
| 앵커 낱말 | `football` | `golf` |

**`gl=US` 로 두면 `football` 이 미식축구로 나온다.** 그래서 축구만 영국 로케일이다.

**축구는 재포장 채널도 후보다.** 춤은 학원·개인이 찍은 원본이 있어 허락을 구할 상대가
있지만, 축구는 화면이 중계권자 것이라 **허락받을 원본 채널이 애초에 없고** 짹짹 자체가
같은 자리에 있다. 레이더로 돌릴 것은 중계·구단 공식 계정(✖)뿐이다.

## ★실기에서 잡은 함정 넷 (2026-08-21 첫 발굴)

| 함정 | 증상 | 대응 |
|---|---|---|
| **영국 로케일의 소문자 `k`** | `114k views` 를 대문자만 보는 파서가 **114** 로 읽어 후보 0건. 오류는 안 난다 | `views_of()` 를 대소문자 둘 다 받게 |
| 넓히다 주제 낱말을 잃음 | `bizarre moment football match` → **`bizarre`** 까지 깎여 자동차·크리켓 채널이 들어옴 | 넓힐 때 **앵커를 도로 붙인다** |
| 두루뭉술한 맥락 낱말 | `defender`(랜드로버) · `pitch`(야구) · `var`(various) · 골프의 `driver`·`iron`·`green`·`course` | 빼거나 **두 낱말로 묶는다** (`golf course`) |
| `boxing` 맨낱말 차단 | 골프의 `unboxing new driver` 가 통째로 걸림 | `boxing match` 로 묶는다 |

세 번째를 고치니 딴주제 제외가 26 → **55** 로 늘고 오염 채널이 사라졌다.
**로케일을 바꾸면 숫자 표기부터 확인해라.**

## 갈래 배수 — ★아직 실측이 아니다

칩칩은 같은 클립 71쌍을 대조해 뽑았지만 축구·골프는 **그 대조를 안 했다.**
지금 값은 사장님이 말씀하신 채널 성격에 맞춘 것이다 —
축구 `판정·논란 1.35 / 사건·난입 1.30 / 골키퍼 1.20 / 묘기·기술 **0.85**`,
골프 `아이템·장비 **1.35** / 동물·돌발 1.25 / 신기·희귀 1.20 / 묘기·기록 0.90`.

`묘기·기술`·`묘기·기록` 을 깎은 것은 칩칩 실측(`실력 자랑 ×0.85`)을 옮긴 것이다 —
**잘하는 클립은 안 터지고 옆에서 벌어진 사건이 터진다.** 회차가 쌓이면 다시 재라.

## 예약

| 작업 | 때 | 하는 일 |
|---|---|---|
| `볼케이노 축구골프 클립 09시30분` | 매일 | 풀 재서 후보 + 폰 알림 (`--src soccer` · `--src golf`) |
| `볼케이노 축구골프 풀넓히기 월요일` | 주1회 04시 | `--scout` 로 풀 넓히기 |
| `볼케이노 축구골프 커뮤 09시` | 매일 | **커뮤 글** 갈래 (`ref_comm --sport`) — 레이더 몫 |

**커뮤 쪽에서 춤은 뺐다** (2026-08-21) — 칩칩 전용 헌터가 소스 채널 풀로 훨씬 잘 잡는다.
같은 일을 두 군데서 하면 헷갈리기만 한다.

## 재사용 스크립트 — 전부 `ref_sport/` 에 있다

`hunt.py` (엔진) · **`topics.py` (주제 정의 — 채널을 늘리려면 여기에 항목을 쓴다)**
· `sheet.py` (썸네일 시트) · `daily.cmd` · `weekly.cmd` · `hunt_spec.md`
· 풀 `pool_<key>.json` · 중복 `seen_<key>.json` · 차단 `block_<key>.json`
· 결과는 `ref_sport/out/soccer/` · `ref_sport/out/golf/`

**`seen_<key>.json` 을 잃으면 이미 쓴 클립을 또 추천한다.** 백업에 들어가 있다.
