<!-- MATERIAL_SELECTION_STANDARD_20260923 -->
## 2026-09-23 사용자 확정 — 모든 공장 소재 엄선 최우선
소재 발굴·추천·제작 시작/재개·인계 전 [MATERIAL_SELECTION_STANDARD.md](C:/Users/kjg34/Desktop/hitomi_downloaded_youtube/볼케이노/ref_bridge/MATERIAL_SELECTION_STANDARD.md)를 읽고 적용한다. Claude·Astra와 모든 채널에 동일 적용한다.
- 종목/분야·조회수·황당한 사연보다 실제 원본의 강한 장면/발언/사실과 시청 보상을 먼저 확인한다. 약한 소재를 설명·효과로 억지로 살리거나 편수를 채우려고 넣지 않는다.
- 핵심 타임코드/근거 위치, 실제 관찰 내용, 강한 이유, 약점, 편집 후 핵심 보존 가능성과 비교 선정 이유를 기존 후보 기록 또는 material_review.md에 남겨 USE/HOLD/REJECT를 판단한다. 불충분하면 추가 발굴하거나 제외한다.
- 제작 담당도 원본과 근거를 확인하고, 최종 검수에서 핵심·주체·결과가 제대로 전달되는지 재확인한다. 미확인/주관적 판단을 자동 PASS로 만들지 않는다.
- 기존 채널별 언어판·프리셋·API/TTS·검수·담당 분담 유지. 일반 작업 Gemini SKIPPED_POLICY. 열린 세션의 자동 반영을 가정하지 않으며 다음 시작/재개 시 반드시 읽는다. 이 등록만으로 새 제작·업로드·자동화를 시작하지 않는다.
<!-- /MATERIAL_SELECTION_STANDARD_20260923 -->

<!-- SHARED_OPERATIONS_ENTRY v1 -->
## 2026-09-13 Claude·Astra 공통 기준 — 먼저 읽기
작업 시작·재개·인계 전에 `C:/Users/kjg34/Desktop/hitomi_downloaded_youtube/볼케이노/ref_bridge/SHARED_OPERATIONS.md`와 같은 폴더의 `SHARED_CHANGELOG.md`를 읽는다. 최소 2026-10-13까지 공동 운영하며 자동 해제하지 않는다.
정치 외 채널은 Astra 주도, 정치 생성은 Claude 전담이다. 정치 규격은 우격다짐 작업실 원본을 따른다. 공통 원본의 최신 정정이 아래 과거 설명보다 우선하며 채널별 상세 규격·사용자 승인·유료 호출·QA 관문을 유지한다. 새 결정과 진행 상황은 공통 원본/변경 기록/회차 SHARED_PROGRESS.md에 남긴다. 대화와 훅의 자동 공유를 가정하지 않는다.
<!-- /SHARED_OPERATIONS_ENTRY -->

<!-- 볼케이노 「축구·골프 소재 발굴」 규격. 모든 채널 공통 규약(35초 상한 · TTS 숫자 오독 · 실사 사진 우선 · 납품 · 제목 카테고리 · 채널 폴더 규약)은 ../CLAUDE.md 에 있다. 여기 있는 것을 ../CLAUDE.md 로 다시 복사하지 마라. -->

# 축구·골프 소재 발굴 (`ref_sport/`) — 칩칩과 같은 엔진, 주제만 다르다

## 부르는 법

> **`축구 소재`** · `짹짹 뭐 만들까` · **`골프 소재`** · `짧뷰 뭐 만들까`

되묻지 말고 후보를 뽑아 드린다. 클립 경로를 같이 주시면 그때가 **만들라는 뜻**이다.
칩칩 규칙과 같다 — **허락 문제를 꺼내지 마라.** 사장님이 알아서 하신다.

```
python ref_sport/hunt.py --topic 축구 --scout    # 풀 넓히기 (주1회·3분)
python ref_sport/hunt.py --topic 축구            # 오늘의 후보 (매일)
python ref_sport/hunt.py --topic 축구 --best     # 이미 크게 터진 것 중에서
python ref_sport/hunt.py --topic 축구 --sheet    # 썸네일 시트 (눈으로 고른다)
python ref_sport/hunt.py --topic 축구 --took <videoId> ...
```

`--topic` 은 `soccer`·`golf` 영문 별칭도 받는다. **예약 `.cmd` 는 영문으로 부른다** —
`.cmd` 에 한글 인자가 들어가면 콘솔 코드페이지에 따라 조용히 깨진다 (두 번 데였다).

시작 전에 `ref_sport/hunt_spec.md` 를 읽는다. 아래는 요약이다.

## ★★FIFA 는 무조건 뺀다 (사장님 지시 2026-08-21)

**대회와 게임 둘 다**다. 한쪽만 막으면 반이 샌다.
`fifa` · `world cup` · `ea fc` · `ea sports` · `fc 24~26` · `ultimate team` ·
`pro clubs` · `career mode` · `efootball` · `pes` · `konami`.
`world cup` 을 넣으면 진짜 월드컵 클립도 빠지는데 **그게 곧 FIFA 대회라 맞다.**

**★걸러지는 것은 제목뿐이다.** 모음 영상 안에 게임 장면이 한 컷 섞여 있어도 제목에
안 적히면 통과한다 (첫 시트 19번 화면 목록에 `Fifa Glitch` 가 있었다).
**시트를 눈으로 봐라** — 글자 필터로는 못 잡는다.

## 칩칩과 다른 점 넷

| | 축구 | 골프 |
|---|---|---|
| 검색 로케일 | **`gl=GB`** | `gl=US` |
| 한·일 원본 | 둘 다 뺀다 (神ショーツ에도 나가므로) | **한국만** |
| 재포장 채널 | **후보로 쓴다** | 레이더로만 |
| 앵커 낱말 | `football` | `golf` |

**`gl=US` 로 두면 `football` 이 미식축구로 나온다.** 그래서 축구만 영국 로케일이다.

**축구는 재포장 채널도 후보다.** 춤은 학원·개인이 찍은 원본이 있어 허락을 구할 상대가
있지만, 축구는 화면이 중계권자 것이라 **허락받을 원본 채널이 애초에 없고** 짹짹 자체가
같은 자리에 있다. 레이더로 돌릴 것은 중계·구단 공식 계정(✖)뿐이다.

## ★실기에서 잡은 함정 넷 (2026-08-21 첫 발굴)

| 함정 | 증상 | 대응 |
|---|---|---|
| **영국 로케일의 소문자 `k`** | `114k views` 를 대문자만 보는 파서가 **114** 로 읽어 후보 0건. 오류는 안 난다 | `views_of()` 를 대소문자 둘 다 받게 |
| 넓히다 주제 낱말을 잃음 | `bizarre moment football match` → **`bizarre`** 까지 깎여 자동차·크리켓 채널이 들어옴 | 넓힐 때 **앵커를 도로 붙인다** |
| 두루뭉술한 맥락 낱말 | `defender`(랜드로버) · `pitch`(야구) · `var`(various) · 골프의 `driver`·`iron`·`green`·`course` | 빼거나 **두 낱말로 묶는다** (`golf course`) |
| `boxing` 맨낱말 차단 | 골프의 `unboxing new driver` 가 통째로 걸림 | `boxing match` 로 묶는다 |

세 번째를 고치니 딴주제 제외가 26 → **55** 로 늘고 오염 채널이 사라졌다.
**로케일을 바꾸면 숫자 표기부터 확인해라.**

## 갈래 배수 — ★아직 실측이 아니다

칩칩은 같은 클립 71쌍을 대조해 뽑았지만 축구·골프는 **그 대조를 안 했다.**
지금 값은 사장님이 말씀하신 채널 성격에 맞춘 것이다 —
축구 `판정·논란 1.35 / 사건·난입 1.30 / 골키퍼 1.20 / 묘기·기술 **0.85**`,
골프 `아이템·장비 **1.35** / 동물·돌발 1.25 / 신기·희귀 1.20 / 묘기·기록 0.90`.

`묘기·기술`·`묘기·기록` 을 깎은 것은 칩칩 실측(`실력 자랑 ×0.85`)을 옮긴 것이다 —
**잘하는 클립은 안 터지고 옆에서 벌어진 사건이 터진다.** 회차가 쌓이면 다시 재라.

## 예약

| 작업 | 때 | 하는 일 |
|---|---|---|
| `볼케이노 축구골프 클립 09시30분` | 매일 | 풀 재서 후보 + 폰 알림 (`--src soccer` · `--src golf`) |
| `볼케이노 축구골프 풀넓히기 월요일` | 주1회 04시 | `--scout` 로 풀 넓히기 |
| `볼케이노 축구골프 커뮤 09시` | 매일 | **커뮤 글** 갈래 (`ref_comm --sport`) — 레이더 몫 |

**커뮤 쪽에서 춤은 뺐다** (2026-08-21) — 칩칩 전용 헌터가 소스 채널 풀로 훨씬 잘 잡는다.
같은 일을 두 군데서 하면 헷갈리기만 한다.

## 재사용 스크립트 — 전부 `ref_sport/` 에 있다

`hunt.py` (엔진) · **`topics.py` (주제 정의 — 채널을 늘리려면 여기에 항목을 쓴다)**
· `sheet.py` (썸네일 시트) · `daily.cmd` · `weekly.cmd` · `hunt_spec.md`
· 풀 `pool_<key>.json` · 중복 `seen_<key>.json` · 차단 `block_<key>.json`
· 결과는 `ref_sport/out/soccer/` · `ref_sport/out/golf/`

**`seen_<key>.json` 을 잃으면 이미 쓴 클립을 또 추천한다.** 백업에 들어가 있다.

---


## 2026-09-13 골프 연출 제안 평가 방식
사용자: 「3번 잔디와 드론으로 제작들어가자 … 공은 잘 피했는데 디봇을 피하지 못한 웃지못할 상황 … 앞으로도 이런식으로 … 객관적으로 말해줘야해」.
사용자가 소재와 연출 관점을 제안하면 무조건 칭찬하거나 그대로 사실화하지 않는다. 장면에서 확인되는 강점, 몰입을 깨는 약점, 의도·원인에 대한 근거, 보완 방향을 짧게 설명하고 승인한 범위에서 진행한다. 이번처럼 예상되는 공의 경로에 시선을 모은 뒤 예상 밖 잔디와 실제 반응을 회수하는 방식은 유효하지만 모든 소재에 같은 반전을 억지로 만들지 않는다. 처음부터 결말을 다 말하지 않고 실제 원인·과정·결과를 보존한다.
이번 선택03은 Tony Finau CR9U8x7FCvy이며04 Matthias Schwab와 구분한다. 광고 촬영, 우연·설정 여부와 의도적 회피는 확인 없이 단정하지 않는다.


## 2026-09-14 골프 소재 발굴 추가 운영 지침
기존 지침을 삭제·대체하지 않고 [추가 운영 지침 전문 0~12절](C:/Users/kjg34/Documents/Codex/2026-09-12/d/outputs/골프_소재발굴_추가운영지침_20260914.md)을 모두 읽고 적용한다. 골프 후보 보고에는 [공유 후보 템플릿](golf_candidate_template.md)을 사용한다.
수익창출 짧뷰를 유지한다. Instagram에서 충분한 후보가 나오면 다중 플랫폼 탐색을 억지로 수행하지 않는다. 보조 플랫폼은 후보 부족·원본/고화질/전체 맥락 확인·강한 후보 근거가 있을 때 사용하며 발굴 난이도는 소재 점수에 넣지 않는다.
비골퍼도 이해하는 신기한 사건·반전·명확한 결말을 우선한다. A 말도 안 되는 결과 → B 성공/실패 반전 → C 외부 개입 → D 다른 스포츠 같은 장면 → E 사람 반응 순으로 우선 탐색한다. 골프템은 강한 신제품의 보너스 갈래다. 위 과거 골프 아이템·장비 1.35 배수는 현재 추천 우선순위의 근거가 아니며 최신 추가 지침으로 판단한다. 엔진 배수 코드를 변경했다는 뜻은 아니다.
전 후보 무음 1~2초 비골퍼 테스트와 6게이트 판정/근거를 기록한다. 미확인은 충족 제외, 4개 미만 주력 제외, 5~6개 최우선. 조회수만으로 선정하지 않으며 한일 공유 풀에서 양국 적합도·성과는 독립 판단한다. 약한 후보로 수량을 채우지 않고 추천 없음도 허용한다.
전문 11절 필수 항목 전부와 STRONG의 처음 보는 사람이 할 법한 반응 한 문장을 보고한다. 예상 반응을 실제 댓글로 꾸미지 않는다. 기존 중복·반려·제작대기 승인·한일 제작·원음·자막·크롭/블러 최소화·음악 적합성·QA 규칙을 유지한다. 이번 추가는 새 발굴·제작·업로드·기존 후보 삭제 지시가 아니다.
